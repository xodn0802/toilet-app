# 화장실 찾기 앱(뿡)

낯선 곳에서 가까운 화장실을 찾고, 청결도·편의시설·이용조건까지 알려주는 지도 웹앱.
상세 기획과 기능 명세(P0/P1/P2), 그리고 **지금 무엇이 되는지**는 @README.md 참고.
기획 배경은 @idea.md 참고. **README 는 무엇이 되는지, 이 문서는 왜 그렇게 했는지다.**

배포: https://toilet-app-azure.vercel.app/ · 저장소: https://github.com/xodn0802/toilet-app

**개발 일지(노션)** — https://app.notion.com/p/3b45662314c4814995d0ee7c678542be
이 문서를 사람이 읽을 수 있게 날짜별로 옮겨 적은 것이다. **결정의 근거는 이
CLAUDE.md 가 원본이고 노션은 사본이다** — 새 작업이 끝나면 노션도 같이 갱신한다.

## 진행 상황 (2026-08-17 기준)

- 완료 1단계 — 프로젝트 셋업 + Vercel 배포 (324dd9e)
- 완료 2단계 — Supabase 스키마 toilets·reviews·review_photos + RLS (c2d32a6)
- 완료 P0 1·3번 — 지도 홈: 현재 위치 + 화장실 마커 + 마커 상세 시트 (9b37f1e)
  - 흰 화면 버그 해결. 아래 "지도 레이아웃 주의" 참고.
- 완료 P0 4번 — 도보 길찾기 (6d29534)
  - TMAP 보행자 경로 API로 좌표를 받아 카카오맵 Polyline으로 그린다.
    상단 배너와 상세 시트에 "279m · 도보 4분" 표시. 실시간 안내는 하지 않는다.
  - 카카오모빌리티 길찾기는 **자동차 전용**이라 못 쓴다. 카카오맵 JS SDK에는
    경로 탐색 기능 자체가 없다. 그래서 좌표는 TMAP, 그리기만 카카오다.
- 완료 데이터 소스 교체 — mock 제거, Supabase 조회로 전환 (5db971a)
  - `lib/toilets/fetch-toilets.ts`가 `geocode_status='ok'` 행을 조회한다.
  - 개발용 표본 6건은 **지웠다.** 아래 "공공데이터 적재" 절 참고.
- 완료 앱 정체성 — 청자 색 체계 + 워드마크 앱바 + 리뷰 UI (a74815f)
  - 색은 `app/globals.css` 한 곳에만 있다. 아래 "색은 globals.css 에서만" 참고.
  - 리뷰 UI(별점·태그)는 이때 **화면만** 만들었다. 저장은 P0 5번에서 붙였다.
    사진 칸은 지웠다 — 아래 "익명 인증" 절 참고.
  - 화면을 만들 때 쓰던 **가짜 리뷰는 지웠다 (8045b7d, 2026-08-03).** 지도가
    공공데이터로 바뀐 뒤로는 진짜 화장실 위에 지어낸 별점이 얹히는 꼴이라
    데이터 전체가 의심스러워진다. 지금은 모든 화장실이 "아직 리뷰가 없어요"다.
    `reviewsByToilet` state 와 `handleSubmitReview` 는 남겨 뒀고, P0 5번에서
    그 자리에 reviews 테이블 조회·저장이 들어갔다.
- 완료 배포 정상화 (c6ce8e0) — Vercel에 Supabase 두 변수 등록 후 재배포.
  배포된 앱에서 마커 6건·콘솔 에러 0 확인(2026-07-31).
- 완료 위치 오차 표시 (d1e3be5) — `accuracy`가 1km를 넘으면 파란 점 대신 오차
  반경 원. 아래 "현재 위치가 부정확한 문제" 참고. **실제 데스크톱에서 이게
  켜지는지는 확인 못 했다** — 같은 절의 "남은 의문"을 먼저 읽을 것.
- 완료 반경 표시 (67d46ee) — 앱바가 `1km 이내 5곳`처럼 기준과 함께 말하고, 반경
  밖 마커는 흐려진다. 아래 "반경 표시" 절 참고. **배포까지 확인함(2026-08-01)** —
  프로덕션에서 네 경우 전부 재현, 마커 6건·콘솔 에러 0.
- 취소 P0 7번 로그인 — 카카오 소셜 로그인 (161a717). KOE205 로 막혀 있었고
  **익명 인증으로 대체했다(2026-08-05).** 코드는 지웠다. 아래 "익명 인증" 절 참고.
- 완료 3·4·6단계 — 공공데이터 수집 + 지오코딩 + bbox 조회 (cafe194·d347387·023b0d4)
  - **인천 2,130건이 실제 좌표로 지도에 뜬다.** 표본 6건은 지웠다.
  - 아래 "공공데이터 적재" 절에 수집 조건·실측치·재실행 방법이 있다.
- 완료 사용자 화장실 추가 요청 (7497a06·2204443·e30f9f8·260cd98, 2026-08-04)
  - 마이그레이션 적용·RLS 검증·**프로덕션 흐름 확인까지 끝났다.** 실제로 제보가
    접수되고, 검토 전이라 지도에는 안 뜬다. 콘솔 에러 0.
  - 아래 "사용자 화장실 추가 요청" 절 참고.
- 완료 P0 5번 리뷰 등록 + 익명 인증 (de991bc, 2026-08-05)
  - **로그인 화면이 없다.** 리뷰를 등록하는 순간 익명 신원이 만들어지고,
    남용은 신원이 아니라 규칙(1인 1리뷰)이 막는다. 별명은 앱바에 칩으로 보인다.
  - 카카오는 걷어냈지만 `linkIdentity` 로 나중에 얹을 수 있다.
  - 아래 "익명 인증" 절 참고.
- 완료 근접 제한 제거 + 별명 노출 (8874912, 2026-08-05) — **배포까지 확인함**
  - **200m 근접 규칙을 지웠다.** 우회는 쉬운데 정상 사용자만 막고 있었다.
  - "로그인이 왜 안 보이냐"는 지적에는 **로그인을 되살리는 대신** 별명을 앱바
    칩과 리뷰 폼 문구로 드러내는 것으로 답했다(사용자가 고른 방향).
  - 익명 인증 절의 "근접 제한은 지웠다"·"별명을 화면에 보인다" 참고.
  - 프로덕션 재확인 — 직선 1.8km 화장실에 리뷰 등록됨, 앱바에 별명 칩,
    새로고침 후에도 남음, 재시도는 `이미 리뷰를 남기셨어요`, 콘솔 에러 0.
    검증에 쓴 리뷰·익명 계정은 지웠다(당시 리뷰 0건. **지금은 2건이다** —
    인하대역(수인선)에 ★4·★3. 이 문서가 한동안 "0건"이라고 적어 두고 있었다).
- 완료 Stitch 디자인 이식 (e568743·b3f9cc6·7187381·1036b00, 2026-08-07)
  - **UX 다듬기의 첫 조각이다.** 색은 안 바꿨다 — Stitch 가 배포된 앱을 긁어
    만든 것이라 팔레트가 이미 같았다. 바꾼 것은 형태 언어다.
  - 아래 "Stitch 디자인 이식" 절 참고.
- 완료 Stitch 화면 구조 이식 (1c714f9·be0ef0d·0f2b89c·66db7e7, 2026-08-07)
  - **위 이식의 "데스크톱 레이아웃은 안 만든다"가 뒤집혔다.** 사이드바 ·
    소개 페이지(`/about`) · 데스크톱 상세 카드 · 테마 3단 토글이 생겼다.
  - 아래 "Stitch 화면 구조 이식" 절 참고. **뒤집힌 이유가 거기 있다.**
- 완료 README 상태 반영 (cf3a60b, 2026-08-08) — 코드 변경 없음
  - README 가 초기 명세 그대로여서 **밖에서는 무엇이 완성됐는지 알 수 없었다.**
    P0·P1·P2 표에 **상태 칸**을 붙이고 "0. 지금 되는 것"을 맨 위에 놓았다.
  - **이제 상태가 두 곳에 적힌다. 역할을 갈라 두지 않으면 어긋난다** — README 는
    **무엇이 되는지**(바깥에 보이는 얼굴), 이 문서는 **왜 그렇게 했는지**(근거)다.
    기능이 끝나면 README 는 상태 칸 한 글자만 고치고 긴 설명은 여기에만 쓴다.
  - 고치다 찾은 것 — **기술 스택 표에 TMAP 이 빠져 있었다.** 도보 경로를 실제로
    담당하는데 없었다. "카카오맵 (또는 네이버)" 같은 기획 단계의 유보도 확정값으로
    바꿨다. 명세가 실제와 갈린 곳(화면 10개 → 지도 한 화면)도 그대로 적었다.
- 완료 출시 준비 (32af74a·d09f3c7·485fae7·de25221·1520b02·111179c·217d2c9·
  90c90fd·aba6062, 2026-08-10)
  - **인천 2,130곳 → 전국 51,131곳.** 마커 겹침을 목록 시트로 풀고, 「바로 안내」
    버튼 하나로 가깝고 깨끗한 곳까지 경로를 그린다. 방문 통계·링크 미리보기·
    아이콘·개인정보 처리방침까지 붙였다.
  - 아래 **"출시 준비"** 절 참고. **아직 사용자가 해야 할 일 세 가지가 남아 있고
    그것도 거기 적혀 있다.**
- 완료 배포 (2026-08-15) — `0004` 마이그레이션 적용 후 10커밋을 push 했다.
  Vercel 배포 성공, 프로덕션에 전국 데이터·「바로 안내」가 올라가 있다.
  **남은 사람 몫 셋 중 마이그레이션만 끝났다** — 인하대 CSV·Web Analytics 는 아직.
- 완료 인하대 부지 단위 3건 삭제 (2026-08-15) — **커밋 없는 DB 변경이다.**
  `51,131 → 51,128곳`. 아래 "인하대 부지 단위 3건을 지웠다" 절 참고.
- 완료 현재 위치 버튼 수리 (7bb2ef8, 2026-08-16) — 「바로 안내」 줄이 그 버튼을
  덮고 있었다. 아래 "현재 위치 버튼이 안 눌렸다" 절 참고.
- 완료 캠퍼스 준비 — 남/여 구분 + 목록 정렬 + 제보 유도 + 좌표 링크
  (ff7d76d·73ccd8e·0023d93·6b9a9e8, 2026-08-17)
  - 인하대 홍보를 앞두고 **캠퍼스가 비어 있어서** 한 일이다. 새 화면은 하나도
    안 만들었다 — 2026-08-10 에 만든 겹침 목록 시트가 그대로 층 목록이 된다.
  - 아래 **"캠퍼스 — 남/여 구분과 학생 제보 유도"** 절 참고.
- 완료 배포 + 인하대 시드 투입 (2026-08-18) — `0005` 적용(사용자) 후 push,
  Vercel 배포 성공. 이어서 `npm run seed:campus` 로 **캠퍼스 2곳**이 올라갔다.
  아래 "인하대 화장실을 눌러도 아무것도 없다" 절 참고 — **버그가 아니라 빈
  데이터였다.** 남은 사람 몫은 **Vercel Web Analytics 하나**다.

### 배포 깨짐 해결됨 (2026-07-31)

프로덕션이 "NEXT_PUBLIC_SUPABASE_URL 과 NEXT_PUBLIC_SUPABASE_ANON_KEY 가
필요합니다"로 죽어 있었다. 5db971a로 데이터 소스를 Supabase로 바꿨는데
Vercel에 두 변수를 등록하지 않아서였다. 사용자가 등록했고, c6ce8e0 배포에서
**마커 6개·콘솔 에러 0**으로 정상 동작을 확인했다.

교훈 — 클라이언트에서 새 환경변수를 쓰기 시작하면 **push 전에 Vercel 등록
여부를 먼저 확인한다.** 로컬 `.env.local`에 있다고 배포되는 것이 아니다.
그리고 `NEXT_PUBLIC_*`은 빌드 때 번들에 박히는 값이라, 변수만 추가하고
재배포하지 않으면 **기존 배포는 그대로 깨져 있다.**

### 배포에서 지도가 안 뜬다 — 원인 확정됨 (2026-07-31)

**배포별 URL이라서 맞았다.** 사용자가 본 주소는
`toilet-iby4ufu5h-xodn0802s-projects.vercel.app` 이었다. `curl` 로 Referer 만
바꿔 카카오 SDK 에 요청해 확인했다.

| Referer                       | 응답                                                     |
| ----------------------------- | -------------------------------------------------------- |
| `toilet-app-azure.vercel.app` | **200** — SDK 정상                                       |
| `toilet-iby4ufu5h-….app`      | **401** `domain mismatched! ... registered web domains.` |

**등록으로 해결하지 말 것.** 배포별 URL 은 매 배포마다 해시가 바뀌어 등록해도
다음 배포에 또 깨진다. 확인은 항상 프로덕션 URL 로 한다. Vercel 대시보드의
Visit 버튼이 배포별 URL 로 보내는 것이 함정이다. (그 URL 은 Vercel SSO 뒤에
있어서 로그인한 본인에게만 보인다 — 302 → `vercel.com/sso-api`.)

재현 방법 — 배포된 HTML 에서 키를 뽑아 Referer 만 바꿔 두 번 부른다.
`curl -s <프로덕션> | grep -o 'sdk\.js?appkey=[a-f0-9]*'` 로 키를 얻고,
`curl -H "Referer: <검사할 URL>" "https://dapi.kakao.com/v2/maps/sdk.js?appkey=$KEY"`.

아래는 원인을 찾기 전에 적어 둔 기록이다.

### (기록) 배포에서 지도가 안 뜬다는 제보 (2026-07-31, 당시 원인 미확정)

사용자가 Vercel 배포에 접속하니 **"지도를 불러오지 못했습니다. 카카오 개발자
사이트에 이 도메인이 등록되어 있는지 확인해 주세요."** 가 떴다.

확인된 사실 — **`toilet-app-azure.vercel.app` 은 정상이다.** 같은 시각에
브라우저로 열어 마커 6개, SDK 요청 200, 실패 문구 없음을 확인했다.
그러니 **사용자가 본 화면은 다른 호스트일 가능성이 크다.**

이 문구는 `ToiletMap.tsx` 의 `<Script onError>` 에서만 뜬다. 즉 SDK
**스크립트 자체를 못 받았다**는 뜻이고, 카카오는 미등록 도메인에서 온 요청에
스크립트를 주지 않는다. 앞서 `localhost:3111` 에서 같은 문구가 뜨고 `3000`
에서는 정상이었던 것도 같은 이유다 — **호스트·포트가 정확히 일치해야 한다.**

가장 유력한 원인 — Vercel은 배포마다 URL을 여러 개 만든다.

| 종류     | 예                                 | 바뀌나          |
| -------- | ---------------------------------- | --------------- |
| 프로덕션 | `toilet-app-azure.vercel.app`      | 고정            |
| 브랜치   | `toilet-app-git-main-….vercel.app` | 고정            |
| 배포별   | `toilet-app-<해시>-….vercel.app`   | **매 배포마다** |

대시보드의 배포 목록이나 Visit 버튼으로 열면 **배포별 URL**로 가기 쉽다.
카카오에는 프로덕션 URL만 등록돼 있을 것이므로 그 화면만 지도가 죽는다.

다음 세션에서 할 일 — **순서대로.**

1. 그 화면의 **주소창 호스트를 정확히 확인한다.** 이게 먼저다.
2. `toilet-app-azure.vercel.app` 이었다면 원인이 다르므로 다시 판단한다
   (네트워크 차단, 광고 차단 확장, 회사망 등).
3. 다른 호스트였다면 카카오 > 내 애플리케이션 > 플랫폼 > Web 에 등록한다.
   **배포별 URL은 매 배포마다 바뀌므로 등록해도 소용없다.** 고정된 두 개
   (프로덕션·브랜치)만 등록하고, 확인은 항상 프로덕션 URL로 하는 편이 낫다.

같이 고치면 좋을 것 — 지금 문구는 원인을 도메인 하나로 단정한다. 실제로는
네트워크 문제일 수도 있다. `location.host` 를 문구에 같이 보여주면 이번처럼
"어느 주소로 봤는지"를 되묻지 않아도 된다.

### 현재 위치가 부정확한 문제 (2026-07-31 확인)

데스크톱에서 열면 실제 위치(인하대)와 4km 떨어진 곳(송현근린공원)이 찍힌다.
`ToiletMap.tsx`의 위치 코드는 정상이고, **브라우저가 그 좌표를 준 것이다.**
데스크톱은 GPS가 없어 WiFi·IP로 추정하는데, IP만 남으면 ISP 기지국 위치가 나온다.
휴대폰(GPS)으로 열면 정확한지 먼저 확인할 것.

문제는 지금 코드가 `position.coords.accuracy`(오차 반경, m)를 **아예 안 본다는**
점이다. GPS면 10-50m, IP 기반이면 수천 m 이상이 들어온다. 그래서 4km 떨어진
지점을 파란 점으로 자신 있게 찍고, **길찾기 출발지도 그 틀린 위치가 된다.**
"279m · 도보 4분"이 실제로는 4km일 수 있다.

**A안으로 해결함.** `ACCURACY_LIMIT_M = 1000`(GPS 10-50m, WiFi 수백 m, IP 수 km
사이가 여기서 갈린다)을 넘으면 `locationCoarse`가 되고 네 가지가 바뀐다.

1. 파란 점 대신 **오차 반경 원**(`kakao.maps.Circle`). 점은 "여기 있다"라서
   수 km 벗어난 좌표에 찍으면 거짓말이 된다. 원은 "이 안 어딘가"다.
2. **축척을 원에 맞춘다.** 반경이 화면보다 크면 지도가 통째로 주황이 돼
   고장난 것처럼 보인다. `handleRecenter`도 같은 규칙을 따른다.
3. `현재 위치가 부정확합니다 · 오차 반경 약 4.0km` 안내(signal 톤).
4. **상세 시트의 직선거리를 감춘다.** 출발지가 수 km 불확실한데 "555m"라고
   세 자리로 적으면 없는 정밀도를 있는 것처럼 말하게 된다.

길찾기는 **막지 않고 경고만 한다**(`RouteBanner`의 `originUncertain`). 막으면
데스크톱에서 핵심 기능이 통째로 죽고, 임계값 1km가 틀렸을 수도 있다.

남은 선택지 — 아직 안 했고, 필요해지면 그때 한다.

- **B.** `watchPosition`으로 더 정확한 값이 오면 갱신한다. 처음엔 IP였다가 곧
  WiFi로 정밀해지는 경우를 잡는다.
- **C.** 지도를 눌러 출발지를 직접 지정한다. 실내·지하까지 대응.

검증 방법 — Playwright로 `accuracy`를 주입하면 데스크톱에서 재현된다.
`newContext({ permissions: ["geolocation"], geolocation: { latitude, longitude,
accuracy } })`로 4000과 30을 각각 넣어 두 경로를 다 확인했다. 카카오는 Circle을
`<ellipse>`로 그리고 **색을 `style` 속성에 넣는다** — `fill`·`stroke` 속성을
보면 못 찾는다. MCP의 Playwright는 `chrome` 채널을 찾다 실패하니
`~/.cache/ms-playwright/chromium-*/chrome-linux64/chrome`를 직접 지정할 것.

**남은 의문 — 실제 데스크톱에서 이게 켜지는지 확인 못 했다 (2026-08-01).**
배포 후 사용자 데스크톱에서 **오차 원이 안 보였다.** 배포는 정상이다 —
번들(`_next/static/chunks/*.js`)에 "부정확"·"출발지가 다를 수 있습니다"가 둘 다
들어 있는 것을 확인했다. 그러니 그 브라우저의 `accuracy`가 **1000 이하로
보고됐다는 뜻**이다. 실제 값은 못 받았다(콘솔에 아무것도 안 떴다고 함).

급하지 않아 미뤘다. 다시 문제가 되면 **먼저 실제 `accuracy` 값부터 받을 것.**

- 정확히 `1000` 이면 경계 문제다. `> ACCURACY_LIMIT_M` 을 `>=` 로 바꾸면 된다.
  크롬이 IP 기반 추정에 딱 1000 을 반환하는 경우가 있다.
- 1000 보다 작은데 위치가 km 단위로 틀리면 **A안의 전제가 깨진 것이다.**
  브라우저가 오차를 축소 보고하는 것이라 임계값을 낮춰도 못 잡는다. 그때는
  B안(`watchPosition`)이나 C안(출발지 직접 지정)으로 방향을 바꾼다.

교훈 — `accuracy` 가 얼마나 정직한지 확인하지 않고 임계값 1km 를 정했다.
Playwright 로 주입한 값은 내가 정한 값이라 이 가정을 검증해 주지 못한다.

### UI에서 바로 고칠 두 가지 (2026-07-31 사용자 요청)

**둘 다 끝났다.** 2번(반경)은 2026-08-01, 1번(다크 모드 토글)은 2026-08-07 에
66db7e7 로 했다. 아래는 그때의 조사 기록이고, **결론은 그대로 구현됐다** —
`light-dark()` · 3단 · `<head>` 인라인 스크립트 전부. 달라진 곳은 붙인 자리
하나뿐이다(`AppBar` → 사이드바 하단, `components/ThemeToggle.tsx`).

**1. 다크 모드 토글 버튼이 없다.** 지금은 OS 설정(`prefers-color-scheme`)만
따라가서, 사용자가 앱 안에서 바꿀 방법이 없다. 화면에 버튼이 안 보이는 게
당연한 상태다. 붙일 곳은 `AppBar.tsx`.

- 값은 `라이트 / 다크 / 시스템` 3단이어야 한다. 2단으로 만들면 "OS를 따라간다"는
  현재 동작으로 되돌아갈 수 없다.
- **첫 페인트 전에** 저장값을 적용해야 흰 화면이 번쩍이지 않는다
  (`layout.tsx`의 인라인 `<script>`). localStorage는 이 용도로는 써도 된다 —
  저장 금지 대상은 TMAP 경로 응답이지 화면 설정이 아니다.

**착수 전 조사까지는 끝냈다 (2026-08-01). 코드는 아직 안 건드렸다.**

CSS 는 원래 여기 적어 둔 "`[data-theme="dark"]` 블록을 `@media` 와 같은 값으로
하나 더 두는" 방식 **대신 `light-dark()` 를 쓸 것.** 값을 두 벌 적으면 이 문서가
경고한 "두 모드가 서서히 어긋난다"가 그대로 벌어진다.

```css
:root {
  color-scheme: light dark; /* 없으면 light-dark() 가 항상 라이트로 풀린다 */
  --surface: light-dark(#ffffff, #17211e);
}
:root[data-theme="light"] {
  color-scheme: light;
}
:root[data-theme="dark"] {
  color-scheme: dark;
}
```

색마다 한 줄이고 두 값이 나란히 있어 어긋날 수가 없다. `data-theme` 가 없으면
`light dark` 라 OS 를 따라가므로 **"시스템" 이 속성 없음으로 자연히 표현된다.**

Chromium 에서 6가지 조합(OS 라이트·다크 × 속성 없음·light·dark)을 전부 확인했다.
**Tailwind 가 `bg-surface/95` 를 `color-mix(in oklab, …)` 로 바꾸는데 그 안에서도
정상으로 풀린다** — 이게 유일한 걱정거리였고 문제 없었다.

주의 — 토글 버튼의 아이콘은 SSR 때 저장값을 모른다. 인라인 스크립트는 `<html>`
속성만 고치지 버튼 내용은 못 고치므로, 마운트 전에는 중립 아이콘을 두고 마운트
후에 실제 값을 반영해야 hydration 불일치가 안 난다(next-themes 도 같은 방식).
**색 자체는 스크립트가 처리하므로 번쩍이지 않는다.** 잠깐 어긋나는 건 아이콘뿐.

참고 문서 — `node_modules/next/dist/docs/01-app/02-guides/preventing-flash-before-hydration.md`
에 이 주제가 통째로 있다. `<head>` 안 `dangerouslySetInnerHTML` 스크립트 +
`<html>` 에 `suppressHydrationWarning` 이 공식 방식이다.

`viewport.themeColor` 는 지금 `prefers-color-scheme` 로만 갈리므로, 수동으로
바꾸면 주소창 색이 안 따라온다. 범위 밖으로 두되 알고는 있을 것.

**2. "주변 N곳"에 범위가 없었다 — 해결됨. 아래 절 참고.**

### 반경 표시 (2026-08-01 완료)

앱바가 세던 숫자는 조회해 온 전부였다. 지도를 옮겨도 안 변하는 숫자를
"주변"이라 부르면 기준을 알 수 없다. 고정 반경 1km(`NEARBY_RADIUS_M`)를 정하고
문구에 같이 적는다.

| 상태                           | 앱바 문구                          | 마커                 |
| ------------------------------ | ---------------------------------- | -------------------- |
| 목록 로딩 중                   | `불러오는 중…`                     | —                    |
| 기준점 있음 · 반경 안에 있음   | `1km 이내 5곳`                     | 안쪽 진하게, 밖 흐림 |
| 기준점 있음 · 반경 안에 없음   | `1km 이내 없음 · 가까운 곳 30.6km` | 전부 흐림            |
| `denied` 또는 `locationCoarse` | `화장실 6곳`                       | 전부 진하게          |
| 목록 0건                       | `등록된 곳 없음`                   | —                    |

**기준점을 쓸 조건은 `anchored` 하나다** — `locationState === "granted" &&
!locationCoarse`. 상세 시트의 직선거리도 같은 값을 본다. 한쪽만 감추면 "거리는
못 알려주면서 1km 이내라고는 하는" 화면이 된다. 오차 원 때와 같은 원칙이다:
**기준점을 못 믿으면 반경을 주장하지 않는다.**

- 계산은 `lib/toilets/nearby.ts`의 `summarizeNearby` 하나에 있다. 문구(`summary`)와
  마커(`nearIds`)를 **함께 돌려주는 이유**는 각자 계산하면 "3곳이라 써 놓고 진한
  핀은 4개"로 어긋나기 때문이다.
- 반경 밖 마커는 **숨기지 않고 흐리게**(`opacity 0.45`) 둔다. 급한 사람에게 1.1km
  화장실을 안 보여주는 건 손해다. **선택은 반경보다 우선한다** — 먼 곳을 골라도
  진하고 크게. `MARKER_Z`가 `far(1) < normal(2) < selected(3)`인 것도 같은 이유다.
- 0곳일 때 가장 가까운 거리를 같이 적는 이유: "없음"만 쓰면 막다른 길이 된다.
- `fetch-toilets.ts`는 **일부러 안 건드렸다.** 공공데이터가 적재되면 이 걸러내기는
  DB 조회 조건(bbox, 6단계)으로 내려가야 하는데, 지금 조회를 손대면 그때 두 번
  고치게 된다.

검증 — Playwright로 `accuracy` 30/4000·권한 없음·서울시청 좌표 네 경우를 확인했다.
로컬과 **프로덕션에서 같은 결과**를 얻었고 콘솔 에러는 0건이었다.

**마커의 흐림은 `<img>`의 data URI 안에서 `opacity="0.45"`를 찾아야 한다**(카카오는
마커를 `<img>`로 그린다). 반경 밖 핀은 기본 축척(level 4)에서 화면 밖이라 클릭
검증을 하려면 휠로 두 단계 줄여야 `boundingBox()`가 잡힌다. 카카오는 마커 클릭을
자체 이벤트로 처리하므로 **DOM 의 `.click()` 은 안 먹는다** — `page.mouse.click()`
으로 실제 좌표를 눌러야 한다.

검증 환경 두 가지 함정.

- **`playwright` 는 이 프로젝트에 설치돼 있지 않다.** MCP 플러그인이 받아 둔 것을
  빌려 쓴다: `NODE_PATH=~/.npm/_npx/*/node_modules node 스크립트.js`, 브라우저는
  `~/.cache/ms-playwright/chromium-*/chrome-linux64/chrome` 를 `executablePath` 로
  직접 지정한다.
- **서버를 내릴 때 `pkill -f "npm start"` 를 쓰지 말 것.** 자기 셸의 명령줄에도
  그 문자열이 있어 자신을 먼저 죽인다. 게다가 `next start` 는 기동 후 프로세스
  이름이 `next-server (v16.x)` 로 바뀌어 `next start` 로는 안 잡히고, SIGTERM
  으로도 안 죽는다. 포트에서 PID 를 찾아 `kill -9` 하는 것이 확실하다 —
  `ss -ltnp | grep :3000`.

### 공공데이터 적재 (3·4·6단계, 2026-08-03 완료)

인천 공중화장실 **2,130건이 실제 좌표로 지도에 뜬다.** 표본 6건은 지웠다.

> **이 절의 숫자와 "지역 인자" 설명은 2026-08-03 당시의 것이다.** 2026-08-10 에
> **전국 51,131곳**으로 늘리면서 `npm run collect` 의 기본이 전량 수집으로 바뀌었다.
> 아래 "출시 준비" 절의 "전국 수집" 항목이 최신이다. **API·키·함정 설명은 그대로
> 유효하다.**

**API** — 행정안전부 공중화장실정보 조회서비스(`data.go.kr/data/15155058`).
엔드포인트는 `https://apis.data.go.kr/1741000/public_restroom_info_v2/info_v2`.
개발계정 일 10,000건, `numOfRows` **최대 100**.

- **엔드포인트를 웹 검색으로는 못 찾았다.** 상세 페이지 HTML 을 받아 안에
  박힌 swagger JSON 을 뽑아야 나온다 —
  `curl -s 'https://www.data.go.kr/tcs/dss/selectApiDataDetailView.do?publicDataPk=15155058'`
- **디코딩 키를 쓴다.** `URLSearchParams` 가 한 번 인코딩하므로 인코딩 키를
  넣으면 `%2B` 가 `%252B` 가 돼 `SERVICE_KEY_IS_NOT_REGISTERED_ERROR` 가 난다.
- **좌표를 안 준다.** 2025년 2월 원천데이터 정책 변경으로 WGS84 제공 중단.
  그래서 4단계 지오코딩이 선택이 아니라 필수다.
- 남녀공용 여부 필드가 **아예 없다.** `unisex: null` 로 두고 UI 는 "정보 없음"
  으로 보여준다. 장애인 화장실도 유무 필드가 없어 대변기·소변기 수에서 끌어낸다.

**실행** — 두 스크립트 모두 여러 번 돌려도 안전하다.

```
npm run collect          # 인천. 지역을 바꾸려면 npm run collect -- 부산
npm run geocode          # pending 인 것만. 중단해도 이어서 된다
```

`.env.local` 에 `DATA_GO_KR_SERVICE_KEY`·`KAKAO_REST_API_KEY`·
`SUPABASE_SERVICE_ROLE_KEY` 가 필요하다. **셋 다 Vercel 에는 올리지 않는다.**
service role 키가 필요한 이유는 `toilets` 에 RLS 쓰기 정책이 없어서다.

**실측 (인천, 2026-08-03)**

| 단계     | 수치                                                     |
| -------- | -------------------------------------------------------- |
| 수집     | 받은 2,276 → 넣은 2,166 (미개방 108 · 다른 지역 2 제외)  |
| 지오코딩 | 성공 2,130 (98.3%) · 실패 36 (결과없음 26 · 대표점만 10) |

실패 36건은 `geocode_status='failed'` 로 남아 지도에 안 뜬다. 다시 시도하려면
`update public.toilets set geocode_status='pending' where geocode_status='failed';`
후 `npm run geocode`. **주소가 그대로면 결과도 같으므로 보통은 의미가 없다.**

**걸러낸 것 셋.** 왜 걸렀는지가 중요하다.

- **미개방 108건** — 학교·사설 빌딩. 시설은 있는데 일반인이 못 들어간다.
  지도에 찍으면 급한 사람을 헛걸음시킨다. "불규칙" 41건은 **남긴다** — 열려는
  있고 시간만 일정하지 않다는 뜻이라 판단을 사용자에게 맡긴다.
- **다른 지역 2건** — API 의 `cond[...::LIKE]` 는 주소 **어디에든** 맞으면
  잡는다. "인천"으로 받으면 "충청남도 논산시 양촌면 **인천**리"가 딸려 온다.
  좌표는 정확히 논산에 찍히므로 지오코딩으로는 절대 안 걸러진다. 한국 주소는
  시도명으로 시작하니 **접두 일치**로 거른다.
- **`address_type` 이 `ROAD`·`REGION` 인 지오코딩 결과 10건** — 도로와 동의
  중심점이라 건물 위치가 아니다.

**좌표가 같은 무리는 정상이다.** 인천 최다는 31개가 한 점(**국립환경과학원**.
한동안 "캠퍼스"라고 적어 뒀는데 실제로 세어 보니 아니었다), 그 다음이
인천대공원 13개다. 원 데이터의 주소가 부지 단위라 건물 구분이 없어서지 지오코딩
오류가 아니다 — 대표점(`REGION`)은 이미 걸렀다. **겹쳐 하나만 눌리는 문제는
2026-08-10 에 풀었다** — 아래 "마커 겹침" 절 참고.

**bbox 조회 — 상한에 잘리는 함정.** `fetchToilets(viewport, anchor)` 가 지도
영역만 조회한다. 조회 영역에 `현재 위치 ± 1km` 를 합치는데, **합치는 것만으로는
부족했다.** 상한(`MAX_ROWS` 300)에 걸리면 정렬이 없어 기준점 주변이 통째로
잘려 나갈 수 있다 — 인천 전역까지 축소하니 앱바가 `1km 이내 32곳` 에서
`7곳` 으로 줄었다. 그래서 **상한에 닿았을 때는 기준점 주변만 다시 온전히 받아
합친다.** 상한에 닿으면 "확대해서 보세요"도 함께 안내한다.

검증 — Playwright 로 인하대 좌표에서 ①마커가 실제 위치(인하대역·홈플러스·
정석학술정보관) ②이동·축소해도 앱바 32곳 유지 ③상세·길찾기(722m·도보 10분)
④콘솔 에러 0 을 확인했다.

**Supabase 조회는 기본 1,000건에서 잘린다.** 검증 스크립트로 전량을 셀 때
이걸 몰라 "인천 밖 0건"이라고 한 번 오판했다. `.range(from, from+999)` 로
페이지네이션할 것.

**스크래치 스크립트는 `NODE_PATH` 가 필요하다.** 스크래치패드가 프로젝트 밖에
있어 `@supabase/supabase-js` 를 못 찾는다 —
`NODE_PATH=/home/teo/toilet-app/node_modules npx tsx --env-file=.env.local <파일>`.

### 사용자 화장실 추가 요청 (2026-08-04 완료, 배포됨)

지도 오른쪽 아래 `+` → 지도를 움직여 위치를 맞춤 → 정보 입력 → **"개발자가
검토한 뒤 지도에 추가하겠습니다"**. `README.md` P2 의 "사용자 화장실 직접 추가".

공공데이터에는 상가·카페 화장실이 **아예 없다.** 급할 때 실제로 쓰는 곳이
그건데 지도에는 공중화장실 2,130건뿐이라 사용자가 채우게 한다.

**바로 지도에 안 뜬다.** 검증 없이 올리면 장난 등록 하나가 급한 사람을
헛걸음시킨다. 그래서 `toilets` 가 아니라 `toilet_submissions` 검토 큐에 쌓는다.
(`toilets` 에 status 컬럼을 붙이는 방법도 있었지만, 그러면 조회 조건·부분
인덱스를 전부 손봐야 하고 검토 전 데이터가 지도 조회 경로에 섞인다.)

**신원을 요구하지 않는다.** 원래는 카카오 로그인이 KOE205 로 막혀 있어서였고,
지금은 익명 인증이 있지만 **그대로 둔다** — 제보는 검토 큐를 거치므로 작성자를
알 필요가 없고, 신원을 만들지 않으면 제보만 하는 사람에게 계정이 안 생긴다.
대신 RLS 를 **"등록만 되고 읽기는 안 됨"**으로 둔다 — INSERT 정책만 있고
**SELECT 정책이 없다.**

- supabase-js 의 `.insert()` 는 `.select()` 를 **안 붙였을 때만**
  `Prefer: return=minimal` 로 보낸다. 그래서 SELECT 정책 없이도 등록이 된다.
  `lib/toilets/submit-toilet.ts` 가 `.select()` 를 안 붙이는 이유가 이것이다.
  붙이는 순간 읽기 권한이 없어 실패한다.
- `with check` 가 `status='pending'`·`reviewed_at is null`·`toilet_id is null` 을
  강제한다. 안 막으면 클라이언트가 `status='approved'` 를 직접 넣어 검토를
  건너뛴다.

**승인·반려는 Supabase 대시보드에서 SQL 로 한다.** 승인 UI 는 안 만들었다 —
쿼리는 `supabase/migrations/0002_toilet_submissions.sql` 주석에 그대로 있다.

- `toilets_has_address` 때문에 **주소가 둘 다 null 이면 승인이 실패한다.**
  역지오코딩이 안 된 제보가 여기 해당한다. 주소를 채워 넣고 다시 실행할 것.
- **휴지·비누는 옮겨지지 않는다.** `toilets` 에 컬럼이 없다. 그건 "오늘 있고
  내일 없는" 정보라 `reviews.tags` 의 몫이고, 등록 시점에 박아 두면 `idea.md`
  가 경고한 "크라우드소싱 정보가 썩는 문제"가 그대로 생긴다. 폼에서는 받되
  제보 행에만 남고, `기저귀교환대`·`장애인` 둘만 `toilets` 컬럼으로 간다.
- `toilets.access`(무료/개방/음료구매/유료) 컬럼을 같은 마이그레이션에서
  추가했다. 없으면 승인할 때 그 값이 갈 데가 없어 버려진다. **필터·우선노출
  (P1 9번)은 아직 안 만들었다** — 컬럼만 있다.

**역지오코딩은 지도 SDK 의 services 라이브러리로 한다.** 서버 REST 를 쓰면
`KAKAO_REST_API_KEY` 를 Vercel 에 올려야 하는데 환경변수 표에서 "불필요"로
정리해 둔 키다. JS SDK 는 이미 쓰는 JS 키로 브라우저에서 그냥 된다.
**환경변수 추가가 없는 이유가 이것이다.**

- SDK URL 에 **`&libraries=services`** 가 붙어 있어야 한다(`ToiletMap.tsx`).
  빼면 `kakao.maps.services` 가 undefined 다.
- `coord2Address(경도, 위도, cb)` — **경도가 먼저다.** 뒤집으면 조용히 엉뚱한
  곳이 나온다.
- **도로명주소는 건물이 없으면 null 이다.** 지번만 오는 경우가 실제로 있다
  (검증 중 학익동 595-25). 그래서 둘 다 nullable 이고 UI 도 지번으로 폴백한다.

**화면 구조 — 기존 지도 위에 모드로 얹는다.** 별도 라우트를 만들면 지도가 한 번
더 생성되고 사용자가 보던 위치·축척을 잃는다.

- **위치 지정 핀은 지도 마커가 아니라 CSS 로 화면 중앙에 고정한다**
  (`absolute top-1/2 left-1/2`). 마커로 만들면 지도를 움직일 때마다 좌표를 다시
  세팅해야 해서 흔들린다. 화면 중앙 = `map.getCenter()` 라 그 픽셀이 곧 그 좌표다.
- 주소는 **`idle` 에서만** 다시 읽는다. 드래그 내내 부르면 호출이 폭주한다.
- 읽어 온 주소에 **"어느 좌표의 것인지"(`at`)를 같이 담는다.** `center` 는 idle
  마다 새 객체라 `address.at !== center` 자체가 로딩 표시가 되고, 앞 좌표의
  주소가 새 위치에 붙는 실수가 구조적으로 막힌다. 로딩 boolean 이 따로 없다.
- **마커 클릭 억제는 ref 로 한다.** 클릭 리스너는 마커를 만들 때 state 를
  클로저에 가두므로 `addOpen` 을 직접 읽으면 항상 처음 값(false)이다.
- 지도 객체를 **state 로도** 들고 있는다(`mapInstance`). ref 는 채워져도
  리렌더를 안 일으켜 자식에게 전달되는 시점이 안 생긴다.
- 오른쪽 아래 버튼 둘은 **하나의 세로 스택**이다. 시트에 따라 움직이는 위치
  계산을 버튼마다 두지 않으려는 것. 추가 모드에서는 통째로 감춘다.
- **남녀공용은 예/아니오/모름 3단**이고 기본이 모름(null). 2단으로 만들면 안
  고른 사람이 전부 "아니오"가 된다.
- 전송 실패해도 **폼을 그대로 둔다.** 적은 내용이 날아가면 다시 안 쓴다.
- 50m 안에 등록된 곳이 있으면 **경고만 한다.** 건물이 같아도 층이 다르면 다른
  화장실이라 막지 않는다.

**검증 — 프로덕션까지 확인했다 (2026-08-04).** 마이그레이션은 사용자가 Supabase
SQL Editor 에서 적용했다(Success).

RLS 다섯 가지가 전부 의도대로였다. **이게 이 기능의 핵심이라 다시 손댈 때는 이
표부터 확인할 것.**

| 확인                                     | 결과                                       |
| ---------------------------------------- | ------------------------------------------ |
| anon `insert`                            | 성공                                       |
| anon `select`                            | **0건** (SELECT 정책이 없으므로 에러 아님) |
| anon 이 `status:'approved'` 를 직접 넣기 | **거부 42501**                             |
| anon 의 `insert().select()`              | **거부 42501**                             |
| service_role 조회                        | 필드 그대로 들어와 있음                    |

승인 SQL 도 실제로 돌려 봤다 — `toilets` 삽입·`approved` 연결이 되고, **주소가
둘 다 null 이면 `23514` 로 막힌다**(위에 적어 둔 `toilets_has_address` 그대로다).
검증에 쓴 행은 전부 지웠고 지금 제보는 **0건**이다.

프로덕션(`toilet-app-azure.vercel.app`) 흐름 — 핀·주소 패널, `idle` 후 주소 갱신
(`인하로 100` → `소성로 34` → `노적산로 21` → `학익동 595-25`), 폼, `접수됐어요`,
DB 에 `status=pending`, **`toilets` 의 `source='user'` 0건**(검토 전이라 지도에
안 뜬다), 콘솔 에러 0.

주의 — **인하대 캠퍼스 전체가 도로명 `인하로 100` 하나다.** 지도를 조금 끌면
주소가 안 바뀌는 것처럼 보여 한 번 버그로 오해했다. 200px 이상 끌어 확인할 것.

**안 만든 것** — 승인 UI, 사진 첨부(Storage·신고 절차가 따라붙는다), rate limit
(검토 큐가 이미 막아 준다), 이용조건 필터.

### 익명 인증 (P0 7번을 대체함, 2026-08-05)

**로그인 화면이 없다.** 리뷰를 등록하는 순간 익명 신원이 만들어진다.

**왜 카카오를 걷어냈나.** 로그인의 목적은 리뷰 남용 방지 하나였다. 그런데 카카오는
동의항목 설정(KOE205)에 막혀 있었고, 재개하려면 개인 개발자 비즈 앱 전환까지
필요했다. 그 문턱 때문에 **P0 5번(리뷰 등록)이 통째로 멈춰 있었다.**

**Supabase 익명 인증이 이 매듭을 푼다.** `signInAnonymously()` 가 발급하는 사용자도
`auth.users` 에 진짜 행이 생기고 **JWT 의 role 이 `authenticated`** 다. 그래서
`0001_init.sql` 의 RLS 정책 네 개와 외래키 제약이 **한 줄도 안 바뀌고 통과한다.**
(익명과 정식 계정을 갈라야 할 때가 오면 `auth.jwt()->>'is_anonymous'` 로 가른다.
지금은 전원 익명이라 구분할 대상이 없다.)

**되돌릴 수 있다.** `auth.linkIdentity({ provider: 'kakao' })` 로 익명 계정에
카카오를 붙이면 **`user_id` 가 유지돼 이미 쓴 리뷰가 그대로 따라온다.** 사진
업로드(P0 6번)·즐겨찾기(P1 10번)처럼 "기기를 바꿔도 남아야 하는" 것이 생기는
시점에 얹으면 된다. 카카오 콘솔 설정은 **그때 다시 하면 된다** — 아래
"(기록) 카카오 로그인" 절에 남겨 뒀다.

**신원은 리뷰를 쓰려는 순간에만 만든다.** `lib/auth/use-identity.ts` 의 `ensure()`
가 그 자리다. 마운트할 때 발급하면 **방문할 때마다 `auth.users` 에 유령 계정이
쌓인다.** 마운트 시에는 `onAuthStateChange` 구독만 하고(INITIAL_SESSION 이 저장된
세션을 준다) 새로 만들지 않는다.

- `ensure()` 는 `pending` ref 로 감싼다. 안 그러면 **더블클릭에 계정이 두 개** 생긴다.
- 세션은 state 가 아니라 `getSession()` 으로 다시 확인한다. state 는 낡을 수 있다.
- 별명이 없을 때 폴백은 **고정 문자열 `"익명"`** 이다. 여기서 새 랜덤 별명을 만들면
  같은 사람이 렌더마다 다른 이름으로 보인다.

**별명은 `reviews` 행에 함께 박는다.** `auth.users.raw_user_meta_data` 에도 들어가지만
클라이언트는 **자기 것만** 읽을 수 있어 남의 별명을 못 가져온다. 프로필 테이블을
따로 두는 대신 행에 박으면 목록 조회가 조인 없이 끝난다. 위조는 RLS 로 못 막지만
표시용이라 피해가 없고, 화면이 깨지지 않도록 **길이만 20자로 제한**한다.

**별명을 화면에 보인다 (2026-08-05).** "로그인 기능이 왜 안 보이냐"는 지적에 대한
답이다. 로그인을 되살리는 대신, 내 글이 어떤 이름으로 남는지를 보이게 했다.

- **앱바 오른쪽 칩** (`AppBar.tsx`) — `identity.who` 가 있을 때만 그린다. 신원은
  리뷰를 쓰는 순간 생기므로, 칩이 그때 나타나는 것 자체가 구조를 말해 준다.
- **리뷰 폼 하단 문구** (`ReviewForm.tsx`) — 아직 신원이 없는 사람에게는 칩이 안
  보이므로, "무슨 이름으로 남는지"를 알려 줄 자리가 여기뿐이다. 별명이 있으면
  `{별명} 으로 남겨집니다`, 없으면 `등록하면 별명이 자동으로 생겨요`.

**남용은 신원이 아니라 규칙이 막는다.** 익명 신원은 브라우저 데이터를 지우면 새로
만들 수 있어서 신원만으로는 아무것도 못 막는다. 막는 것은 **한 화장실에 하나** 뿐이고,
`reviews_one_per_user_idx` (unique) 가 DB 에서 강제한다.

**근접 제한은 지웠다 (2026-08-05, `far` 판정 제거).** 원래 "200m 안에 있는 사람만"
이라는 규칙이 하나 더 있었다. **막지도 못하면서 정상 사용자만 막고 있었다.**

- 판정이 `lib/reviews/eligibility.ts` 즉 **클라이언트에만** 있어서 위치 권한을 끄면
  그대로 우회됐다. 서버에서 검증하려면 `service_role` 키가 Vercel 에 올라가야 하는데
  그건 이 프로젝트 원칙에 어긋난다.
- 좌표를 못 믿을 때(`locationCoarse`)는 아예 건너뛰는 예외까지 있었다. 데스크톱은
  좌표가 km 단위로 틀리는 일이 흔해서(위 "현재 위치가 부정확한 문제") 그 예외가
  없으면 리뷰 기능이 통째로 죽었기 때문이다.
- 반면 지도를 보다가 리뷰를 남기려는 사람은 실제로 막혔다. 사용자가 배포된 앱에서
  이걸 지적했다.

**다시 붙이지 말 것.** 위치를 자격 심사에 쓰려면 서버 검증이 전제인데, 그러면 키
정책부터 바뀐다. 지금은 unique 인덱스 하나로 충분하다고 판단했다.

검증(로컬 프로덕션 빌드) — 현재 위치를 인하대로 고정하고 지도를 끌어 **직선 1.8km**
떨어진 화장실(인천학익여자고등학교)을 열었다. 버튼이 `리뷰 등록하기` 로 활성화되고
등록까지 됐다. 같은 화장실 재시도는 여전히 `이미 리뷰를 남기셨어요` 로 막히고
(unique 인덱스는 그대로다), 위치 권한을 거부한 상태에서도 폼이 열린다. 콘솔 에러 0.

**IP 로 신원을 만들지 않은 이유.** 사용자가 처음 제안한 방향이었는데, IP 는 신원이
될 수 없다. CGNAT·공용 와이파이면 **모르는 사람들이 한 사람으로 묶여** 서로의 리뷰를
지울 수 있고(삭제 정책이 `user_id = auth.uid()` 다), 이동하면 같은 사람이 다른
사람이 된다.

**사진은 이번에 UI 째 감췄다.** `photos` 필드와 `MAX_PHOTOS` 도 지웠다. 익명 신원에는
부적절한 사진의 책임을 물을 수 없어서(`idea.md` 유의사항 ①) **P0 6번에서 신고·삭제
절차와 함께 되살린다.** 항상 `[]` 로 두면 없는 구조를 있는 척하게 된다.

**대시보드에서 켠 것** — Authentication > Sign In / Providers > **Allow anonymous
sign-ins**. 안 켜면 `signInAnonymously()` 가 **422 `Anonymous sign-ins are disabled`**
로 거부된다. Vercel 환경변수는 **추가 없다.**

**검증 (2026-08-05).** 마이그레이션은 사용자가 SQL Editor 에서 적용했다.

RLS·제약 일곱 가지가 전부 의도대로였다. **다시 손댈 때는 이 표부터 확인할 것.**

| 확인                              | 결과                                          |
| --------------------------------- | --------------------------------------------- |
| `signInAnonymously()`             | `is_anonymous=true` · `role=authenticated`    |
| 익명 세션 insert                  | 성공                                          |
| **같은 화장실에 두 번째 insert**  | **거부 `23505`** (`reviews_one_per_user_idx`) |
| 로그아웃(anon 키) insert          | **거부 `42501`**                              |
| 다른 익명 신원이 남의 리뷰 delete | **0건 삭제**                                  |
| 21자 nickname                     | **거부 `23514`** (`reviews_nickname_length`)  |
| 본인 리뷰 delete                  | 1건 삭제 (정책이 과하게 막지 않는다)          |

브라우저(로컬 + 프로덕션) — 앱바에 로그인 버튼 0개, 리뷰 폼이 잠금 없이 열림,
사진 UI 없음, 별점·태그·한마디 등록 후 목록에 자동 별명(`용감한 수달`)으로 뜸,
**새로고침해도 남아 있음**(DB 에 실제 저장), 같은 화장실 재시도 시
`이미 리뷰를 남기셨어요`, 위치 권한 거부해도 안 막힘, 콘솔 에러 0.
검증에 쓴 리뷰·익명 계정은 전부 지웠다. (당시에는 2.5km 밖에서 근접 제한에 막히는
것도 확인했는데, **그 제한은 이후 지웠다** — 위 "근접 제한은 지웠다" 참고.)

**검증 스크립트의 함정 둘.**

- **마커 클릭은 `page.mouse.click()` 이어야 한다**(카카오 자체 이벤트). 그리고
  **뷰포트 밖 마커를 고르면 클릭이 30초 타임아웃 난다** — `boundingBox()` 가 음수
  좌표를 그대로 돌려주기 때문. 후보를 화면 안으로 걸러야 한다.
- 스크래치 스크립트는 **CJS 로 쓸 것.** `NODE_PATH` 는 ESM 해석에 안 먹어서
  `.mts`/top-level await 로 쓰면 `ERR_MODULE_NOT_FOUND` 가 난다. `.ts` + `async
main()` 또는 `.cjs` + `require` 로 쓰고 `node_modules/.bin/tsx` 를 직접 부른다.

### (기록) 카카오 로그인 — 나중에 `linkIdentity` 로 얹을 때 볼 것

**코드는 지웠다**(`lib/auth/use-auth.ts`, 앱바 로그인 UI). 아래는 **콘솔 설정**
기록이라 다시 붙일 때 그대로 쓸 수 있다. Supabase 쪽은 이미 다 돼 있고 카카오
콘솔의 동의항목 두 줄만 남았었다.

| 상태 | 어디        | 무엇                                                                                     |
| ---- | ----------- | ---------------------------------------------------------------------------------------- |
| ✅   | 카카오 콘솔 | 앱 설정 > 앱 키 > Redirect URI 에 `https://<ref>.supabase.co/auth/v1/callback`           |
| ✅   | 카카오 콘솔 | 같은 화면에서 **Client Secret 발급 + 활성화**                                            |
| ✅   | 카카오 콘솔 | 제품 설정 > 카카오 로그인 > **활성화 ON**                                                |
| ✅   | 카카오 콘솔 | 동의항목 `profile_nickname`                                                              |
| ❌   | 카카오 콘솔 | 동의항목 `profile_image` — **비즈 앱 없이 바로 켤 수 있다. 이것부터 하면 된다.**         |
| ❌   | 카카오 콘솔 | 동의항목 `account_email` — **개인 개발자 비즈 앱 전환**이 먼저다 (사업자등록번호 불필요) |
| ✅   | Supabase    | Authentication > Providers > Kakao 켜고 **REST API 키 + Client Secret**                  |
| ✅   | Supabase    | 같은 화면의 **"Allow users without an email" 켜기**                                      |
| ✅   | Supabase    | URL Configuration > Site URL = 프로덕션 URL, Redirect URLs 에 `http://localhost:3000/**` |

**이메일을 안 받는 선택지는 없다.** Supabase 의 카카오 provider 는 스코프 세 개를
하드코딩해 두고 우리가 준 값을 **덧붙이기만 한다.** 빼는 수단이 아예 없다. 실측:

```
요청: /auth/v1/authorize?provider=kakao&scopes=profile_nickname
결과: scope=account_email+profile_image+profile_nickname+profile_nickname
```

그래서 `signInWithOAuth` 의 `options.scopes` 로는 **못 고친다.** 카카오 콘솔에서
세 항목을 전부 켜는 것이 유일한 길이다. `account_email` 은 앱 설정 > 일반 >
비즈니스 정보 > **개인 개발자 비즈 앱**(본인인증만, 사업자등록번호 불필요) 전환 후
**선택 동의로** 켠다 — 필수로 하면 거부한 사람은 가입 자체를 못 한다.

**두 증상을 구별할 것.** 어디까지 갔는지가 주소창에 그대로 나온다.

| 화면                             | 멈춘 곳           | 뜻                                       |
| -------------------------------- | ----------------- | ---------------------------------------- |
| 날 JSON (`Unsupported provider`) | `*.supabase.co`   | Supabase 에 카카오 provider 가 꺼져 있다 |
| `잘못된 요청 (KOE205)`           | `kauth.kakao.com` | 카카오 동의항목이 안 켜져 있다           |

KOE205 는 **"설정하지 않은 동의 항목"을 그 화면이 직접 이름으로 알려준다.**
설정을 바꾼 뒤 브라우저 없이 검사하려면
`curl -s -i "https://<ref>.supabase.co/auth/v1/authorize?provider=kakao&redirect_to=<앱주소>"`
의 `location:` 헤더에 들어 있는 `scope` 를 보면 된다.

**다시 붙일 때는 `signInWithOAuth` 가 아니라 `linkIdentity` 다.** 새로 로그인시키면
**새 `user_id` 가 생겨 이미 쓴 리뷰와 끊긴다.** 그리고 그때는 익명과 정식 계정이
섞이므로 `auth.jwt()->>'is_anonymous'` 로 가르는 정책이 필요해질 수 있다.

**프로덕션 확인 스크립트의 함정.** `waitUntil:"networkidle"` 직후에 마커를 세면
0 이 나온다 — Supabase 조회가 아직 안 끝났기 때문이다. 이걸로 한 번 "배포가
깨졌다"고 오진했다. **`waitForTimeout(6000)` 을 붙여야 실제 상태가 보인다.**

### Stitch 디자인 이식 (2026-08-07 완료)

사용자가 Google **Stitch** 로 뽑은 디자인 초안 4종(`stitch_/`)을 지금 화면에
입혔다. 커밋 넷 — e568743(토큰) · b3f9cc6(아이콘) · 7187381(상세 시트) ·
1036b00(오버레이·리뷰).

**색을 갈아엎는 작업이 아니었다.** Stitch 가 **배포된 앱을 긁어서** 만든 것이라
`clean-teal #2E7D6B` 가 `--brand` 와, `porcelain-white #F2F5F3` 가 `--paper` 와
**같은 값**이다. 그래서 가져온 것은 색이 아니라 **형태 언어**다 — 청록 그림자,
label-caps 섹션 헤더, 칩, 세그먼트 미터, 선형 아이콘.

사용자가 정한 방향 넷. **되돌리기 전에 이걸 먼저 볼 것.**

| 항목              | 결정                                                  |
| ----------------- | ----------------------------------------------------- |
| 데스크톱 레이아웃 | **안 만든다.** 단일 전체화면 지도 유지, 비주얼만 이식 |
| 브랜드명          | **뿡 유지** (Stitch 의 "FlushFinder Pro" 는 버렸다)   |
| 미구현 기능       | **있는 것만 다시 입힌다.** 껍데기 UI 를 만들지 않는다 |
| 아이콘            | **인라인 SVG.** 웹폰트 추가 없음                      |

**`stitch_/` 는 `.gitignore` 에 넣었다. 커밋 여부와 별개로 반드시 거기 있어야
한다** — Tailwind v4 는 프로젝트 안을 자동으로 훑되 `.gitignore` 는 건너뛴다.
빼 두면 초안 HTML 의 클래스가 프로덕션 CSS 에 그대로 딸려 들어간다
(**실측 85K → 68K**). 같은 이유로 **Tailwind 는 CSS 파일 자체도 훑는다** —
`globals.css` 주석에 `shadow-` 문자열을 적었더니 쓰레기 `.shadow-` 유틸리티가
생겼다. 주석에도 유틸리티처럼 생긴 문자열을 쓰지 말 것.

**그림자 토큰의 이름에 `elev` 를 붙인 이유.** `:root` 변수를 `--shadow-*` 로
지으면 Tailwind v4 의 **유틸리티 네임스페이스와 부딪힌다.** 그래서
`--elev-float`(root) → `--shadow-float`(`@theme inline`) 로 잇는다 —
`--paper` → `--color-paper` 와 같은 방식이다.

- 값은 `rgba(46,125,107,·)`. 중성 회색 그림자는 지도 타일 위에서 때가 탄 것처럼
  보이는데 청록이 섞이면 떠 있는 면과 지도가 같은 세계에 있는 것처럼 붙는다.
- **다크에서는 검정으로 바꾼다.** 어두운 바닥에서 청록 그림자는 아예 안 보인다.
- 쓴 곳 일곱. 앱바·FAB 둘·RouteBanner(float) / 상세시트·LocationPickPanel·
  AddToiletForm(sheet) / Notice(chip). **계획서는 넷이라고 했는데 실제로는
  일곱이었다.**
- `AddToilet.tsx` 의 `drop-shadow` 는 **일부러 남겼다.** 화면 중앙 핀 그래픽에
  거는 SVG 필터라 마커와 같은 부류이지 "떠 있는 면"의 고도가 아니다.

**`text-label` 에 `uppercase` 를 안 붙였다.** 한글에 안 먹는다. 12px·700·자간
0.05em 만으로 본문과 충분히 갈리고, 영문 대문자를 흉내 내려다 한글만 어정쩡해지는
것보다 낫다. 섹션 헤더 다섯(시설 정보·이용자 확인·청결도·리뷰 쓰기·리뷰 N)에 쓴다.
**폼 안의 필드 라벨은 `text-sm` 그대로 둔다** — 컨트롤 바로 위에 붙는 라벨이라
역할이 다르고, 둘을 같게 만들면 위계가 사라진다.

**대비를 조였다** — `--muted` `#6b7672` → `#5a6562`(라이트 `--paper` 위에서
4.29:1 이라 AA 4.5 미달이었다. 지금 약 5.5:1), `--line` `#e2e7e4` → `#d3dbd7`.
다크는 이미 충분해서 안 건드렸다.

#### 아이콘 — 이모지를 걷어냈다 (`components/Icons.tsx`)

Material Symbols(Outlined 400)의 path 를 인라인한다. **Apache 2.0** 이고,
`currentColor` 라 다크 모드와 칩 3상태 색을 그대로 따라간다. 마커 SVG 와 같은
방식이다.

- **원본 viewBox 는 `0 -960 960 960` 이다.** `0 0 24 24` 가 아니다. 옮기려면
  좌표를 전부 다시 계산해야 하고 틀려도 알아채기 어려워 그대로 뒀다.
- 실제 SVG 는 여기서 받는다 —
  `https://fonts.gstatic.com/s/i/short-term/release/materialsymbolsoutlined/{이름}/default/24px.svg`
- **path 를 기억으로 쓰지 말 것.** 한 번 지어냈다가 렌더해 보고 잡았다. 받은
  파일과 기계적으로 대조하는 스크립트를 돌려 여덟 개를 전부 확인했다.

**리뷰 태그(휴지·비누·비데·온수)에는 아이콘이 없다. 계획을 뒤집은 것이다.**

- Material Symbols 에 **두루마리 휴지가 아예 없다**(`toilet_paper`·`tissue`·
  `napkin`·`roll_paper` 전부 없는 이름이다). `dry` 는 손 건조기라 뜻이 어긋난다.
- 직접 그려 봤다. 작은 크기에서 **카메라로 읽혔다.** 설계 6종(A~F)을 더 만들어
  전부 렌더했더니 카메라·메달·전화기·자물쇠·"0h"·"Oh" 로 읽혔다.
- 넷 중 하나만 글자로 두면 그 줄이 통째로 어색해진다. 그래서 **선을 이렇게
  그었다 — 공공데이터가 보증하는 시설에는 아이콘, 이용자가 남긴 태그는 글자.**
  마침 `ToiletFacts` 가 두 정보를 나눠 보여주는 이유와 같은 선이다.
  **다시 시도하지 말 것.** 이 판단은 `Icons.tsx` 주석에도 남겨 뒀다.

#### 상세 시트 — 값과 뜻을 다른 자리에 둔다

- **헤더** — 평균 별점을 회색 한 줄에서 빼내 오른쪽 **teal 배지**(`4.3 ★`)로
  세우고 이름을 키웠다(`text-xl`). 접힌 시트에서 이 값이 "여기 들어갈지 말지"를
  가르는 유일한 판단 재료인데 거리·리뷰수와 같은 회색으로 섞여 있었다.
  남은 줄은 개수(`리뷰 3개`)와 거리만 맡는다.
- **시설 정보** — 2칸 그리드 카드를 **칩 행**으로 바꿨다. 세로를 덜 먹어
  접힌 시트(`max-h-56` → `max-h-64`)에서도 보인다. 펼치지 않고 판단할 수 있는
  정보가 늘어난다.
  - **3상태(있음/없음/정보 없음)는 그대로다.** Stitch 는 긍정 항목만 그리므로
    **이 부분만 일부러 안 따랐다.** 공공데이터의 빈 값을 "없음"으로 말하면 있는
    장애인 화장실을 없다고 하게 된다.
  - **상태를 색이 아니라 글자로 나른다.** 색만으로 구분하면 색을 못 가리는
    사람에게 3상태가 1상태가 된다. 점선 외곽선은 그 위에 얹는 보조 신호다.
- **청결도 미터(신규, `CleanlinessMeter.tsx`)** — 5칸 막대 + 한마디
  (아주 깨끗/깨끗/보통/아쉬움/나쁨). 배지가 값을 말하고 여기가 뜻을 말한다.
  값은 이미 있는 `averageCleanliness` 를 그대로 쓴다.
  - **리뷰 0건이면 5칸 전부 회색 + "아직 평가 없음".** 채워질 자리가 보이면
    비어 있다는 사실이 고장이 아니라 초대로 읽힌다. **지금 DB 의 리뷰는 0건이라
    이게 거의 모든 화장실의 기본 화면이다** — 아래 "다음 할 일"이 지적한 빈 상태
    문제를 이게 직접 개선한다.
  - `reviews === undefined`(조회 중)와 `[]`(진짜 없음)의 구분은 기존 규칙 그대로.
- **칩 언어를 하나로 맞췄다.** 채워진 청록 = 확인된 것, 외곽선 = 아닌 것/모르는
  것. 그래서 `ReviewList` 의 태그 칩도 회색에서 `brand-soft` 로 바꿨다 — 그건
  다녀온 사람이 "봤다"고 말한 **긍정 진술**인데 회색이면 미선택 칩과 같은
  얼굴이었다.

#### Notice 의 `busy` 스피너

위치 확인은 몇 초씩 걸리는데 글자만 있으면 앱이 멈춘 것과 구분되지 않는다.
`border-current` 라 `tone`(muted/signal/error)을 그대로 따라간다 — 여기서만
색을 박으면 주황 칩 위에 청록 머리가 얹힌다.

**지도 로딩 스피너(`ToiletMap.tsx` 안쪽)와 테두리 방식이 다른 것은 의도다.**
그쪽은 배경이 고정이라 `border-line` + `border-t-brand` 로 충분하지만, 이 칩은
tone 에 따라 바닥색이 바뀐다. 검증 스크립트를 쓸 때 **`.animate-spin` 만으로
고르면 지도 로딩 쪽이 먼저 잡힌다** — 칩 텍스트로 좁혀야 한다.

#### 검증 (2026-08-07)

**리뷰가 있는 화면은 Supabase 응답을 가로채서 봤다.** `page.route('**/rest/v1/
reviews*')` 로 가짜 3건을 돌려준다. **DB 에 검증용 행을 쓰고 지우지 않는
편이 낫다** — 프로덕션 데이터고, 지우는 것을 잊으면 그대로 남는다.

라이트/다크 × 리뷰 0건(실제)/3건(가짜) 네 경우, 390×844.

| 확인               | 결과                                             |
| ------------------ | ------------------------------------------------ |
| 지도 컨테이너 높이 | 844 (흰 화면 회귀 없음)                          |
| 마커               | 42개                                             |
| 0건 — 배지·미터    | 배지 없음 · 5칸 전부 `--line` · `아직 평가 없음` |
| 3건 — 배지·미터    | `4.3 ★` · 4칸 `--brand` · `깨끗`                 |
| 3건 — 태그 집계    | 휴지 2 · 비누 2 · 장애인 1, 칩은 `brand-soft`    |
| 접힌 시트          | 시설 칩 두 개가 페이드 위로 온전히 보임          |
| 섹션 헤더          | 다섯 개 전부 12px/700                            |
| 가로 스크롤        | 없음 (390 = 390)                                 |
| 콘솔 에러          | **0**                                            |

스피너는 `getCurrentPosition` 을 **3초 늦춰** `pending` 을 붙잡아 따로 확인했다
(그냥 두면 창이 몇 ms 라 안 잡힌다). `spin 1s` · 12px · 링이 `--muted` · 머리
투명 · 위치가 들어오면 칩째 사라짐. 라이트·다크 양쪽.

**안 한 것과 이유** — 데스크톱 사이드바·상단 네비(사용자 결정, DESIGN.md 본문도
"모바일 우선"이라 화면 쪽이 문서와 모순) · Community·About 페이지(라우트가 없다) ·
검색·필터·저장·공유(P1) · 알림 벨·프로필·"Level 4 Contributor"(익명 인증이라
프로필 개념 자체가 없다) · **사진**(P0 6번, `idea.md` 유의사항 ①) ·
**등록 폼 `비밀번호 필요` 옵션**(`idea.md` 유의사항 ②가 명시 금지) ·
**Inter 웹폰트**(한글 글리프가 없어 어차피 시스템 폰트로 폴백한다) ·
`accessibility-blue`(4번째 색 계열 도입 비용 > 얻는 것) ·
**마커 재디자인**(티어드롭의 뾰족한 끝이 좌표를 정확히 가리키고, 한 점에 최대
31개가 겹치는 상황에서 이름 라벨을 달면 화면이 무너진다).

### Stitch 화면 구조 이식 (2026-08-07 완료)

커밋 넷 — 1c714f9(사이드바 셸) · be0ef0d(`/about`) · 0f2b89c(데스크톱 상세
카드) · 66db7e7(테마 토글).

#### **"데스크톱 레이아웃은 안 만든다"가 뒤집혔다. 사용자가 직접 뒤집었다.**

위 "Stitch 디자인 이식" 절의 결정표 첫 줄이 그것이었다. 그런데 그 이식을
마치고 나니 **첫 화면이 사실상 그대로였다.** 사용자가 원한 것은 형태 언어가
아니라 **화면 구조**였다.

> "내 말은 switch에서 만든 것처럼 사이드바 메뉴 만들고 서비스 소개, 화장실
> 누르면 작은 화면 떠서 상세정보 나오고 메인 지도 화면에서도 사이드 바 같은거
> 있고 뭐 그랬으면 좋겠다고"

**그 표를 다시 근거로 삼지 말 것.** 뒤집힌 것은 그 한 줄뿐이고, 나머지 셋
(**뿡 유지 · 껍데기 UI 금지 · 인라인 SVG**)은 이번에도 그대로 지켰다.

착수 전에 두 가지를 물어 정했다.

| 물음                          | 답                                                           |
| ----------------------------- | ------------------------------------------------------------ |
| 상세를 모바일·데스크톱 다르게 | **그렇다.** 하단 시트 ↔ 왼쪽 카드. **내용·순서·문구는 동일** |
| 소개 페이지에 히어로가 왜     | **유지.** 제목 한 줄 + 두 문장 + `지도 열기` 하나            |

#### 사이드바 (`components/SideNav.tsx`)

**메뉴에 무엇을 넣느냐가 이 작업의 핵심 판단이었다.** Stitch 는 일곱인데
우리에게 있는 것은 셋뿐이라, 그대로 베끼면 **다섯이 죽은 링크가 된다.**

| Stitch              | 우리              | 근거                                            |
| ------------------- | ----------------- | ----------------------------------------------- |
| Find                | **지도** `/`      | 그대로                                          |
| Contributions       | **화장실 등록**   | `AddToilet` 모드가 이미 있다. `/?add=1` 로 연다 |
| —                   | **소개** `/about` | Stitch 의 About                                 |
| Settings            | **테마 3단**      | 미뤄 뒀던 다크 모드 토글이 여기 앉았다          |
| Saved               | 안 만듦           | P1 10. 저장할 곳이 없다                         |
| Report Issue        | 안 만듦           | 신고 기능이 없다. P0 6번(사진)과 같이 와야 한다 |
| Help · Privacy      | `/about` 안 섹션  | 별도 페이지를 만들 만큼 내용이 없다             |
| User Name · Level 4 | **익명 별명 칩**  | 프로필이 없다. `identity.who` 를 그 자리에 둔다 |

- **컴포넌트는 하나다.** 데스크톱 상시 표시와 모바일 드로어를 반응형 클래스로
  가른다(`lg:visible lg:translate-x-0`). 둘로 쪼개면 두 벌이 서서히 어긋난다 —
  `dark:` 를 금지한 이유와 같다.
- 닫혔을 때 `-translate-x-full` 만 주면 **링크가 탭 순서에 남아 안 보이는 곳으로
  포커스가 들어간다.** `invisible` 을 함께 주고 `transition-[transform,visibility]`
  로 전환한다 — `visibility` 는 이산 전환이라 슬라이드는 그대로 보인다.
- **모바일 하단 네비는 안 만들었다.** 상시 56px 을 뺏기면 상세 시트·FAB·오차
  안내가 전부 밀리고, 지도 앱에서 그 자리는 엄지 영역이라 비싸다. 대신 앱바
  왼쪽 메뉴 버튼(`MenuButton`, `lg:hidden`)으로 여는 드로어다.
- 여는 쪽(앱바 안쪽 깊이)과 열리는 쪽(레이아웃)이 떨어져 있어 **문맥으로 잇는다**
  (`SideNavProvider`). `MenuButton` 을 `SideNav.tsx` 가 함께 export 하는 이유다.

**흰 화면(높이 0)을 피하는 구조가 전부다.** `body` 에서 `flex flex-col` 을
**뗐고**, 사이드바는 `fixed` 라 자리를 차지하지 않으며 본문은 `lg:ml-64` 로
비켜설 뿐이다. 세로 flex 사슬을 새로 만들면 지도까지 이어지는 `h-full` 이
무너진다(위 "지도 레이아웃 주의"). `page.tsx` 의 `<main class="flex h-full
flex-col">` 과 `ToiletMap` 내부는 **한 줄도 안 건드렸다.**

**"화장실 등록"이 별도 라우트가 아닌 이유** — 지도를 다른 페이지로 옮기면 지도가
다시 만들어져 보던 위치·축척을 잃는다. `/?add=1` 로 열고, 모드를 닫을 때
`router.replace` 로 쿼리만 지운다.

> **Next 16 에서 `searchParams` 는 Promise 다.** `page.tsx` 가 async 서버
> 컴포넌트가 되고, 그걸 읽는 순간 라우트가 **동적(`ƒ /`)** 으로 바뀐다.

**카카오는 컨테이너 폭이 바뀌면 `relayout()` 을 불러야 한다.** 1024px 경계를
넘나들면 사이드바가 생기고 사라져 지도 폭이 256px 바뀐다. `ToiletMap` 이
`ResizeObserver` 로 컨테이너를 보고 `relayout()` 후 bounds 를 다시 읽는다.
**이번 작업에서 지도 쪽에 필요했던 변경은 사실상 이것 하나다.**

#### 소개 페이지 (`app/about/page.tsx`)

Stitch 의 구조(히어로 · bento 그리드 · 개인정보 카드 · FAQ)를 쓰되 **문장은
전부 실제 동작으로 바꿨다.** 없는 기능을 소개에만 적으면 그게 가장 먼저
거짓말이 된다.

- 숫자는 실측치 하나뿐이다 — 인천 **2,130곳**(`TOILET_COUNT`).
- 편의시설 카드가 **"없음"이 아니라 "정보 없음"** 이라고 적는 이유까지 적어
  뒀다. 3상태를 지키는 이유가 화면과 문서 두 곳에 있어야 안 무너진다.
- 청결도 카드의 5칸 막대는 **`CleanlinessMeter` 를 가져다 쓰지 않았다.** 저쪽은
  리뷰 배열을 받아 계산하는 물건이라 여기서 쓰려면 가짜 리뷰를 지어내야 한다.
  여기 있는 것은 설명용 그림(`aria-hidden`)이다.
- **모바일 전용 `lg:hidden` 헤더에 `MenuButton` 을 둔다.** 이게 없으면 모바일로
  소개에 들어온 사람이 **지도로 돌아갈 길을 잃는다**(사이드바가 안 보이므로).
- `html`·`body` 가 `h-full` 이라 이 페이지 래퍼가 `h-full overflow-y-auto` 로
  스크롤을 직접 맡는다. 지도 페이지는 스크롤이 없어 지금까지 문제가 안 됐다.

**안 쓴 것** — `Level 4 Contributor`(존재하지 않는다) · **비밀번호가 필요한
화장실**(`idea.md` 유의사항 ②가 명시 금지) · 사진 · 검색·필터 자랑(P1) ·
Stitch 의 원격 이미지.

#### 데스크톱 상세 카드 (`ToiletDetailSheet.tsx`)

사용자가 말한 **"화장실 누르면 작은 화면 떠서 상세정보"** 가 이것이다.
**컴포넌트를 쪼개지 않고 반응형 클래스만 얹었다.**

```
absolute inset-x-0 bottom-0 rounded-t-3xl              (모바일 — 그대로)
lg:inset-x-auto lg:top-20 lg:bottom-auto lg:left-4     (데스크톱 — 카드)
lg:w-[400px] lg:rounded-2xl lg:max-h-[calc(100%-7rem)]
```

- **데스크톱에는 접기/펼치기가 없다.** 세로가 충분하다. 핸들바와 `더보기` 는
  `lg:hidden`, 본문은 `lg:overflow-y-auto` 로 늘 스크롤된다. **`expanded` 는
  사실상 모바일 전용 state 가 됐다.**
- 접힌 시트 아래쪽 페이드 그라디언트도 `lg:hidden` 이다. 데스크톱에는 잘리는
  경계가 없어 그냥 얼룩이 된다.
- **겹침 처리는 알림 줄만 손봤다.** FAB 은 오른쪽 아래라 카드(왼쪽)와 안
  겹치므로 `lg:bottom-6` 그대로 두고, 앱바는 `top-20` 위에 있어 안 건드린다.
  카드가 떠 있을 때만 알림 묶음에 `lg:pl-[27rem]`(16+400+16=432px). 그 묶음은
  비었을 때 `gap` 만 남으므로 **`empty:hidden`** 을 붙였다.
- **지도 중심 보정(`panTo` 오프셋)은 안 했다.** 축척마다 픽셀↔좌표 환산이 달라
  따로 다뤄야 한다. 눈으로 봤을 때 실제로 가려지면 그때 별도로 한다.

#### 테마 3단 (`ThemeToggle.tsx` · `globals.css` · `layout.tsx`)

미뤄 뒀던 다크 모드 토글이다. **조사해 둔 결론(`light-dark()`)을 그대로 따랐다** —
위 "UI에서 바로 고칠 두 가지" 절 참고.

- `@media (prefers-color-scheme: dark)` 블록을 **지웠다.** 이제 값이
  `light-dark(라이트, 다크)` 한 줄에 나란히 있다.
- **`data-theme` 이 없는 것이 "시스템"이다.** `color-scheme: light dark` 라
  OS 를 따라간다. 토글은 속성을 붙이거나 뗄 뿐이다.
- **2단이 아니라 3단인 이유** — 2단으로 만들면 "OS 를 따라간다"로 못 돌아온다.
- 첫 페인트 전에 저장값을 붙인다(`layout.tsx` 의 `<head>` 인라인 스크립트 +
  `<html suppressHydrationWarning>`). `useEffect` 로 미루면 라이트로 한 번
  칠해진 뒤 바뀌어 **흰 화면이 번쩍인다.**
- 마운트 전 state 는 `null` 이고, 마운트 후 **`localStorage` 가 아니라
  `documentElement.getAttribute("data-theme")` 를 읽는다.** 실제로 적용된 것이
  무엇인지가 진실이고, 둘을 다 읽으면 어긋날 자리가 생긴다.
- `viewport.themeColor` 는 `prefers-color-scheme` 로만 갈려 **수동 전환 시
  주소창 색이 안 따라온다.** 화면 안은 맞으므로 범위 밖으로 뒀다.

`--elev-cta` 를 추가하고 소개 페이지의 `지도 열기` 에 썼다. 계획서에는 이 토큰만
있고 쓸 곳이 없었는데, **안 쓰는 토큰을 남기느니 앱에서 가장 큰 버튼에 붙였다.**

#### 검증 (2026-08-07)

로컬 프로덕션 빌드, 포트 3000. **콘솔 에러는 모든 화면·폭에서 0.**

| 확인                         | 결과                                                            |
| ---------------------------- | --------------------------------------------------------------- |
| **높이 0 회귀** 390/768/1440 | 지도 컨테이너 844 / 900 / 900 — 무너진 곳 없음                  |
| 1024 경계 왕복               | 1440→900→1440 후 마커 그대로 (`relayout()` 걸림)                |
| 사이드바                     | 데스크톱 상시 · 모바일 숨김 · 메뉴/백드롭/Esc · 현재 항목 강조  |
| 데스크톱 상세 카드           | x=272 y=80 **400×788** · radius 16px · `overflow-y: auto`       |
| 모바일 상세 시트             | **390×256** · 위쪽 radius 24px · 하단 고정 · 핸들·`더보기` 보임 |
| 알림 줄 비켜서기             | `padding-left: 432px` · 비면 `display: none`                    |
| FAB                          | 데스크톱 하단 여백 84px · 모바일 300px(시트를 피한다)           |
| `/about` 모바일 탈출구       | `MenuButton` 1개 · 390 에서 `flex` · 1440 에서 `none`           |
| 가로 스크롤                  | 없음 (390·768·1440 전부)                                        |
| 테마 6조합                   | OS 2 × 선택 3 전부 정확                                         |
| 첫 페인트                    | `readyState:"interactive"` 에 이미 `data-theme="dark"`          |
| `bg-surface/95`              | `oklab(… / .95)` 안에서도 `light-dark()` 정상 해석              |
| 프로덕션 CSS                 | **76,712 B** — `stitch_/` 누출(≈85K) 아님                       |

**검증 스크립트 함정 하나.** `querySelectorAll("div").find(d =>
d.querySelector('button[aria-label="상세 닫기"]'))` 는 **가장 바깥 조상**(지도
컨테이너)을 준다. 시트 기하를 재려면 `btn.closest("div.absolute")` 여야 한다.
한 번 이걸로 `x:256, w:1184` 같은 헛수치를 얻었다.

**최종 확인은 사용자가 브라우저로 해야 한다** — `toilet-app-azure.vercel.app`
은 Vercel 봇 차단이 걸려 자동 접근이 403 이다.

**남겨 둔 것** — 검색바·필터(P1) · Saved·Report Issue(기능 자체가 없다) ·
알림 벨·프로필(익명 인증) · **주변 목록 시트·마커 겹침 묶기**(따로 한다).

### 출시 준비 (2026-08-10 완료)

인하대 에브리타임에 홍보해 실사용자를 받고 **하루에 몇 명이 오는지 숫자로 보는**
것이 목표다. 받은 피드백은 하나였다 — **"들어가자마자 버튼 하나 누르면 가장
가깝고 청결도가 좋은 화장실로 안내"**. 여기에 세 가지가 먼저 필요했다: 인천을
벗어나면 지도가 비었고, 홍보 대상인 인하대 캠퍼스가 통째로 비어 있었고, 접속자
수를 볼 방법이 아예 없었다.

커밋 아홉 — 32af74a(전국 수집) · d09f3c7(건물·층) · 485fae7(겹침 목록) ·
de25221(바로 안내) · 1520b02(캠퍼스 시드) · 111179c(통계) · 217d2c9(OG·아이콘) ·
90c90fd(직선 폴백) · aba6062(처리방침).

**사용자가 해야 할 일 세 가지가 남았다. 아래 "남은 사람 몫" 참고.**

#### 전국 수집 — 시도별로 나눠 받으면 21%를 잃는다

`npm run collect` 가 **인자가 없으면 전국 전량**이다(536쪽). 지역 인자를 주면
기존 동작 그대로다.

시도별 루프를 안 쓴 이유가 이 단계의 핵심이다. 접두 일치가 **두 군데서 샌다.**

- 데이터에 **`전남광주통합특별시`** 라는 통합 개편 지명이 들어와 있다.
  `광주광역시`·`전라남도` 로는 각각 0건·5건만 잡힌다 — **실제로는 4,973건이다.**
- `울산 울주군 …` 처럼 **시도명이 축약된 행**이 섞여 있다.

17개 시도를 다 돌아도 42,195건뿐이라 전체 53,552건 중 **21%를 놓친다.** 전량을
받으면 버려지는 것이 없고, 지역 필터 때문에 있던 `isOutsideRegion` 예외도 같이
사라진다. **`collect-toilets.ts` 가 원래 "전국을 받으면 대부분이 버려진다"고
적어 두고 지역 필터를 골랐는데, 그 판단은 인천만 쓰던 때의 것이다.**

| 단계     | 인천 (2026-08-03) | 전국 (2026-08-10)   |
| -------- | ----------------- | ------------------- |
| 받음     | 2,276             | 53,552              |
| 넣음     | 2,166             | 52,751 (미개방 801) |
| 지오코딩 | 2,130 (98.3%)     | **51,131** (97.0%)  |
| 실패     | 36                | 1,620               |

**지도에 뜨는 수와 적재 수가 다르다.** 화면·문서에 적는 숫자는 항상
`geocode_status='ok'` 인 **51,131** 이다. 52,751 은 사용자가 볼 수 없는 수다.

`geocode-toilets.ts` 는 **한 줄도 안 고쳤다** — `pending` 을 전부 처리하므로
이미 전국 대응이었다. **PostGIS 도 안 넣었다**(`0001_init.sql:12` 이 "전국으로
넓힐 때 재검토"라고 적어 뒀지만, 5만 행은 부분 인덱스로 충분하다).

#### 마커 겹침 — 좌표 완전 일치로만 묶는다 (`lib/toilets/cluster.ts`)

전국 실측 **4,755그룹 13,773곳, 전체의 26.9%.** 최다 71곳(충남대)·54곳
(스타필드하남). 인천만 쓸 때의 "최다 31곳"보다 훨씬 크다.

- **카카오 `MarkerClusterer` 는 이 문제를 못 푼다** — 좌표가 같으면 아무리
  확대해도 안 갈라진다. 픽셀 거리 클러스터링은 축척마다 다시 계산해야 한다.
  겹침의 원인이 "같은 부지 주소 → 같은 지오코딩 결과" 하나라서 **동일 좌표가
  문제의 전부다.**
- **배지는 캔버스를 넓히지 않고 핀 머리 위에 겹친다.** 넓히면 기준점이 밀려
  핀 끝이 좌표를 안 가리킨다. **이름 라벨은 안 단다** — 이름이 전부 부지
  단위라 구분이 안 되면서 화면만 무너진다.
- **1곳짜리는 목록을 거치지 않고 바로 상세다.** 한 줄짜리 목록을 한 번 더
  누르게 하는 것은 손해다.
- **흐림 판정은 묶음 단위다.** 겹친 것 중 하나라도 반경 안이면 진하게. 그
  마커를 흐리게 하면 반경 안 화장실로 가는 유일한 입구가 안 보인다.
- **앱바의 `1km 이내 N곳` 은 묶음이 아니라 곳 수다.** `summarizeNearby` 는
  그대로 뒀다.
- 목록의 평균 별점은 열릴 때 한 번만 받고 **어느 묶음의 것인지를 같이 담는다**
  (상세 시트의 주소 `at` 과 같은 수법). 안 담으면 다음 묶음에 앞 묶음의 별점이
  잠깐 붙는다.

#### 건물·층 (`0004_building_floor.sql`)

`toilets`·`toilet_submissions` 양쪽에 nullable `building`·`floor`. **`floor` 는
`text` 다** — `3`, `B1`, `지하 1층` 이 다 들어온다. 정수로 두면 지하·중층을
못 담는다. 공공데이터는 이 값을 안 주므로 **5만 건이 전부 null 이 기본값이다.**

`0002` 주석의 **승인 SQL 도 같이 고쳤다.** 거기가 유일한 승인 절차라, 안 고치면
제보의 건물·층이 조용히 버려진다.

#### 「바로 안내」 (`lib/toilets/pick-best.ts`)

**1km 이내 거리순 상위 5곳 중 평균 별점이 가장 높은 곳.** 거리가 먼저 자르고
청결도가 그 안에서 고른다 — "위치가 당연히 더 우선"이라는 사용자 판단을
**"거리로 후보를 좁힌 뒤"** 라는 구조로 옮긴 것이다.

- 5보다 넓히면 800m 를 더 걷게 만들 수 있고, 좁히면 청결도가 개입할 자리가 없다.
- 반경은 앱바와 **같은 상수**(`NEARBY_RADIUS_M`)다. 화면이 `1km 이내 5곳` 이라
  말해 놓고 버튼이 3km 밖으로 보내면 두 숫자가 서로를 부정한다.
- 평점이 하나도 없으면 최근접이 된다. 실패가 아니라 "아직 아무도 평가하지
  않았다"이므로 안내는 그대로 하고, **왜 이 곳인지를 배너가 말한다**
  (`가까운 5곳 중 가장 깨끗한 곳` / `가장 가까운 곳`).
- **근거에는 어느 화장실의 것인지를 같이 담는다.** 안 담으면 그 뒤에 마커를
  직접 눌러 길찾기를 해도 앞 문구가 남아, 사용자가 고른 곳을 앱이 고른 것처럼
  말하게 된다.
- `handleNavigate` 를 **`navigateTo(toilet)` 로 바꿨다.** `selected` 를
  클로저에서 읽으면 `setSelected` 직후에는 아직 옛 값이라 엉뚱한 곳으로 그린다.
- 별점은 후보 5곳만 한 번에 받는다(`lib/reviews/fetch-ratings.ts`).
  **집계 컬럼이나 DB 뷰는 안 만들었다** — 리뷰 2건 규모에 집계 인프라를 먼저
  세울 이유가 없다.

#### 캠퍼스 시드 (`scripts/seed-campus.ts` · `scripts/data/inha-toilets.csv`)

인하대 좌표에 잡히는 공공데이터는 `인하대학교`·`인하공업전문대학`·
`정석항공과학고등학교` **세 줄뿐**이고 이름이 부지 단위라 어느 건물인지도 모른다.

> **그 세 줄은 2026-08-15 에 지웠다.** 지금 인하대 캠퍼스는 비어 있다.
> 아래 "인하대 부지 단위 3건을 지웠다" 절 참고 — **재수집하면 되살아난다.**

- **좌표를 표에 적게 하지 않는다.** 사람이 위경도를 뒤집어 적는 사고가 난다.
  카카오 키워드 검색으로 `인하대학교 {건물}` 을 찾아 쓰고, **건물당 한 번만**
  찾아 같은 건물의 층들이 서로 다른 좌표를 갖는 일도 막는다.
- **캠퍼스 밖 결과는 거부한다(중심에서 1.2km).** 이름을 조금만 다르게 적어도
  카카오가 인하공업전문대학이나 타 대학의 같은 이름 건물을 첫 결과로 준다.
  조용히 들어가면 지도에는 멀쩡한데 길찾기가 다른 학교로 안내한다.
- **빈칸은 `n` 이 아니라 "모름"이다.** `n` 을 기본값으로 삼으면 안 적은 건물이
  전부 "장애인 화장실 없음"이 돼 있는 시설을 없다고 말하게 된다.
- `external_id` 는 `inha-{건물}-{층}`. `0001` 은 이 칸을 "사용자 등록 건은
  NULL" 로 적어 뒀지만, **시드는 스크립트가 다시 넣을 수 있어야 하는 것**이라
  여기서는 채운다.
- 같은 건물의 층들이 한 점에 겹치는데 **그것이 설계된 동작이다** — 위 목록
  시트가 그 겹침을 그대로 층 목록으로 펼친다.

#### 방문 통계 · 링크 미리보기 · 아이콘

- `@vercel/analytics` 한 줄(`app/layout.tsx`). **referrer 가 이번 목적의
  핵심이다** — 에브리타임 링크가 실제로 사람을 데려오는지 그것으로만 안다.
  쿠키를 안 써 동의 배너가 없다. **Vercel 대시보드에서 켜야 쌓인다.**
  로컬에서는 `/_vercel/insights/script.js` 가 **404 로 뜨는 것이 정상이다** —
  그 경로는 Vercel 이 배포된 앱 앞에서 붙여 준다. 검증할 때 이 404 는 무시한다.
- **`next/og` 는 라틴 폰트(`Geist-Regular.ttf`) 하나만 안고 있다.** 한글을 그냥
  그리면 **통째로 빈 칸**이 나온다. 그래서 쓸 글자만 담은 서브셋을 `fonts` 로
  직접 넘긴다(`assets/black-han-sans-subset.ttf`, 12KB). 받는 방법은
  `lib/og/brand.tsx` 주석에 있다 — 구글 폰트 `css2?family=…&text=<글자>` 가
  truetype 을 바로 준다. **`fontTools` 는 필요 없다**(설치돼 있지도 않다).
  글자를 새로 쓰면 서브셋을 다시 받아야 한다.
- `metadataBase` 가 없으면 og:image 가 **상대 경로로 나가 미리보기가 통째로
  빈다.** 프로덕션 주소를 박아 둔다 — 배포별 URL 은 다음 배포에 사라진다.
- `app/favicon.ico`(Next 기본값)를 지우고 `app/icon.tsx` 가 대신한다. 생성된
  아이콘 라우트는 **해시 쿼리 없이 `/icon` 으로도 200** 이라 `manifest.ts` 가
  그대로 가리킨다.

#### TMAP 한도 — 직선 폴백 (`RouteState` 의 `straight`)

**TMAP 일 1,000건이 유일한 실질 병목이다.** 방문자 200명이 평균 5회 안내를
받으면 그날 넘긴다. 넘으면 `app/api/walk-route` 가 502 를 주는데, 예전에는 오류
문구만 남고 **지도에 아무것도 안 그려졌다** — 급한 사람이 방향조차 모르게 된다.

직선거리는 이쪽에서 계산하는 값이라 남의 서비스가 어떻든 나온다. 그래서 실패하면
출발지-도착지를 잇는 **점선**을 그리고 `직선 287m` 이라고 말한다.

- **점선이라 "이 길로 가라"가 아니라 "저쪽 방향"으로 읽힌다.**
- **소요 시간은 적지 않는다.** 직선거리로 시간을 지어내면 실제보다 짧게 말한다.
- **실패 상태(`status:"error"`)를 지웠다.** 이제 도달할 수 없다. 실패 이유도
  가리지 않는다 — 한도 초과든 네트워크든 사용자가 할 수 있는 일은 같다.
- 계산에 쓴 출발지를 상태에 담는다. 지도를 옮겨도 그린 선과 숫자가 안 어긋난다.

**메모리 캐시와 한도 상향은 안 했다.** 실사용량을 보고 정한다.
**약관상 24시간 이상 보관 금지라 localStorage·DB 캐시는 애초에 안 된다.**

#### 개인정보 처리방침 (`/about`)

`idea.md` 유의사항 ③ 이 요구하는 "현재 위치 사용에 대한 별도 동의"가 지금까지
브라우저 권한 창뿐이었다. **그 창은 좌표가 어디까지 가는지 말해 주지 않는다.**
별도 페이지를 안 만든 것은 사이드바 메뉴를 정할 때와 같은 판단이다.

**"무엇을 저장하지 않는지" 카드에 방문 통계를 한 줄 넣었다.** 안 모으는 것만
늘어놓고 모으는 것 하나를 아래 고지에만 숨겨 두면 **그게 빠뜨린 거짓말이 된다.**

문의처는 **GitHub 이슈**다. 개인 이메일을 소개 페이지에 박는 것은 되돌리기
어려워서 고르지 않았다 — 바꾸려면 그 자리를 고치면 된다.

#### 남은 사람 몫 — **코드로는 못 하는 것들**

1. **`supabase/migrations/0004_building_floor.sql` 을 SQL Editor 에 붙여 Run.**
   적용 전까지는 **제보 폼 등록과 `npm run seed:campus` 가 둘 다
   `column toilets.building does not exist` 로 실패한다.**
2. **`scripts/data/inha-toilets.csv` 를 현장 조사로 채우고 `npm run seed:campus`.**
   지금 파일은 형식 예시다. **전 건물을 다 채울 필요 없다** — 자주 가는 건물
   5~10개면 홍보에는 충분하고 나머지는 학생들이 제보로 채운다.
3. **Vercel 대시보드에서 Web Analytics 켜기.** 안 켜면 아무것도 안 쌓인다.

그리고 **출시 전에 실기기 확인이 필요하다. 이것이 가장 큰 미검증 리스크다.**
에브리타임 유입은 대부분 **앱 내부 웹뷰**로 들어오는데, 거기서
`getCurrentPosition` 이 막히면 앱이 통째로 무용지물이 된다. 자동화로는 검증할 수
없다. 같이 볼 것 — **실제 `accuracy` 값**("현재 위치가 부정확한 문제"의 남은
의문이 아직 안 풀렸다. 휴대폰 GPS 값을 여기서 처음 받게 된다).

#### 용량·한도

| 항목            | 지금             | 판단                             |
| --------------- | ---------------- | -------------------------------- |
| Supabase DB     | 52,751행 (~20MB) | 무료 500MB 안                    |
| **TMAP**        | 일 1,000건 무료  | ⚠️ 유일한 병목. 위 폴백으로 대응 |
| 카카오 지오코딩 | 5만 건 1회       | 일 100,000건 안                  |
| Vercel          | Hobby            | 대시보드에서 확인                |

### 인하대 부지 단위 3건을 지웠다 (2026-08-15)

**커밋이 없는 변경이다.** Supabase 행을 직접 지웠으므로 git 히스토리에 흔적이
없다. 그래서 여기 적는다 — 나중에 "인하대 마커가 왜 없지"를 물을 곳이 이 문서뿐이다.

지운 것은 한 좌표(`37.4514818862257,126.651350878024`)에 겹쳐 있던 세 행이다.

| 이름                 | external_id                       |
| -------------------- | --------------------------------- |
| 인하대학교           | `mois-3510500-202435105000100864` |
| 인하공업전문대학     | `mois-3510500-202435105000100862` |
| 정석항공과학고등학교 | `mois-3510500-202435105000100933` |

셋이 주소를 `인천광역시 미추홀구 인하로 100` 으로 똑같이 써서 지오코딩 결과가
한 점으로 모였다. **겹침 묶기가 오작동한 게 아니다** — 배지 `3` 은 정확했고,
데이터 쪽이 쓸모없었다. 이름이 캠퍼스 전체를 가리켜 어느 건물인지 알 수 없고
좌표도 정문 근처 한 점이라, 홍보 대상인 인하대생에게 길찾기가 엉뚱한 데로 간다.
`51,131 → 51,128곳`.

**`npm run collect` 을 다시 돌리면 되살아난다.** 수집은 `external_id` 로 upsert
하는데 지워진 행은 **새 행**으로 들어와 `geocode_status` 가 기본값 `pending` 이
되고, 이어서 `npm run geocode` 를 돌리면 다시 `ok` 가 돼 지도에 뜬다. **제외
목록은 안 넣었다** — 세 행 때문에 수집 스크립트에 예외를 새기는 것보다, 되살아난
뒤 다시 지우는 편이 싸다. 그때 쓸 쿼리는 이것이다.

```sql
delete from public.toilets where external_id in (
  'mois-3510500-202435105000100864',  -- 인하대학교
  'mois-3510500-202435105000100862',  -- 인하공업전문대학
  'mois-3510500-202435105000100933'   -- 정석항공과학고등학교
);
```

**같은 문제의 다른 곳은 안 건드렸다.** 인하사대부중·부고 2건이 `재넘이길123번길29`
를 공유해 아직 겹쳐 있다. 전국에 이런 부지 단위 항목이 얼마나 되는지는 안 세어
봤다 — **캠퍼스 데이터를 직접 채우는 인하대만 예외로 다룬 것이다.** 전국을 훑어
지우는 것은 범위가 크고 되돌리기 번거로워 고르지 않았다.

### 현재 위치 버튼이 안 눌렸다 (2026-08-16)

사용자 제보 — **"오른쪽 아래 위치 누르면 아무것도 작동 안 한다."** 원인이 둘이었고
둘 다 고쳤다.

**1. 「바로 안내」 줄이 그 버튼을 덮고 있었다. 이게 진짜 원인이다.**
그 줄은 `absolute inset-x-0 bottom-6 z-10` 이라 **화면 전폭**을 차지한다. FAB 스택도
`bottom-6` 이고 같은 `z-10` 인데, 이쪽이 JSX 에서 나중이라 이겨서 클릭을 가로챘다.
`px-24` 로 버튼끼리는 안 겹쳐 보이지만 **감싸는 줄은 여전히 오른쪽 끝까지 있다.**
위쪽 「추가」 버튼은 더 높아서 안 덮였고, 그래서 **위치 버튼만** 죽어 있었다.

안쪽 버튼에 이미 `pointer-events-auto` 가 붙어 있었다 — **부모가
`pointer-events-none` 일 때만 뜻이 있는 클래스라, 처음부터 짝이 빠져 있던 것이다.**
줄에 `pointer-events-none` 을 주는 것으로 끝났다.

> **지도 위에 전폭 오버레이를 얹을 때는 `pointer-events-none` 을 같이 준다.**
> "지도 레이아웃 주의"의 z-index 규칙과 짝이다 — z-index 를 올려 안 덮이게 하는
> 것과, 덮되 클릭을 통과시키는 것은 다른 문제다. 이번 건은 후자였다.

**2. 위치를 못 받으면 버튼이 `disabled` 였다.** `locationState !== "granted"` 면
잠갔는데, 그러면 **위치를 다시 받을 길이 그 버튼에는 없다** — 다시 묻는
`requestLocation` 은 「바로 안내」만 부르고 있었다. 이제 위치가 없으면 누를 때
다시 묻고, 받으면 `center` 가 바뀌어 기존 effect 가 지도를 옮긴다. 잠그는 것은
**묻는 중(`pending`)일 때뿐**이고 그동안은 안내 칩의 스피너가 상태를 말한다.

검증(로컬 프로덕션 빌드, 390×844) — 거부 상태에서 버튼이 눌리고
`getCurrentPosition` 호출이 **1 → 2** 로 늘었다. 허용 상태에서는 파란 점이 화면
중앙(0px)에 있다가 끌면 **322px** 벗어나고, 버튼을 누르면 **정확히 중앙(0px)** 으로
돌아온다. 「바로 안내」·「추가」도 그대로 눌린다(경로 배너·상세 시트·등록 패널 확인).
콘솔 에러는 `/_vercel/insights/script.js` 404 뿐인데 **로컬에서는 정상이다.**

**검증 요령** — 지도 중심을 재려고 카카오 내부(`Map.prototype.setCenter`)를 가로채
봤지만 안 잡혔다. **화면에 실제로 보이는 현재 위치 점**(`div[style*="9999px"]`)의
`boundingBox()` 를 뷰포트 중앙과 비교하는 편이 확실하고 구현에 안 묶인다.

### 캠퍼스 — 남/여 구분과 학생 제보 유도 (2026-08-17 완료)

인하대 에브리타임 홍보를 앞두고 **캠퍼스가 비어 있었다.** 부지 단위 3건은
2026-08-15 에 지웠고(위 절), 시드 CSV 는 헤더뿐이었다. 사용자가 말한 것은 넷이다 —
건물 단위로 표시하고, 누르면 청결도 순 목록으로 펼치고, 위치를 알려 주고,
**나머지는 학생들이 쉽게 추가하도록 유도한다.**

커밋 넷 — ff7d76d(성별) · 73ccd8e(정렬·그 자리 추가) · 0023d93(점선 핀) ·
6b9a9e8(좌표 링크·시드 2건).

**앞의 둘은 이미 서 있었다.** 사용자가 그린 구조가 2026-08-10 「출시 준비」의
겹침 묶기와 같다 — `groupByCoords` 가 좌표가 같은 것을 마커 하나로 묶고
`ToiletListSheet` 가 목록으로 펼치며, `seed-campus.ts` 는 **건물당 좌표 하나**를
써서 층들을 일부러 겹쳐 넣는다. **새 화면을 하나도 안 만든 이유가 이것이다.**
실제로 빠져 있던 것만 넷 채웠다.

#### `gender` — `unisex` 로는 못 하는 말

`unisex` 는 **"공용이냐"까지만 답한다.** 공용이 아니라는 것은 말해도 그것이
남자용인지 여자용인지는 못 말한다. 공공데이터는 한 건물을 한 줄로 묶어 놔서
그 구분이 필요 없었지만, **캠퍼스 실내 화장실은 같은 층에 남/여가 나란히 있는
것이 기본**이라 "정석학술정보관 지하 1층 **남자**"를 표현할 수가 없었다.

- **`unisex` 를 지우지 않고 파생시킨다** — `unisexFromGender(gender)`.
  `ToiletFacts` 가 이미 `unisex` 를 쓰고 있고 공공데이터 5만 행에는 `gender` 가
  없어서 지울 수 없다. 대신 **입력 지점을 하나로 만들어** 두 컬럼이 서로 다른
  말을 하는 자리를 없앤다. 제보에서 `gender` 를 고르면 `unisex` 는 계산된다.
- 표시는 `gender` 가 있으면 그것, 없으면 `unisex` 로 떨어진다. 그래서 **공공
  데이터 행의 화면은 한 글자도 안 바뀐다.**
- **NULL 이 정상값이다.** `building`·`floor` 와 같은 규칙 — 값이 있을 때만 그린다.
- 값이 셋(`male`/`female`/`all`)뿐이라 `text` + `check` 다. enum 은 값을 더할 때
  타입 변경이 필요한데 이 목록이 늘 일이 없다.
- 제보 폼의 남녀공용 3단(예/아니오/모름)이 **남자/여자/공용/모름 4단**이 됐다.
  기본은 여전히 모름이다.

**`external_id` 에 성별이 들어간다** — `inha-{건물}-{층}-{성별}`. 안 넣으면 같은
층의 남/여가 한 행을 두고 싸워 **upsert 가 뒤엣것으로 앞엣것을 덮어쓴다.**
CSV 가 아직 0행이고 `npm run seed:campus` 를 한 번도 안 돌렸을 때 바꿨다 —
**시드를 넣은 뒤에 규칙을 바꾸면 지우지 못하는 중복이 생긴다.**

**승인 SQL 정본을 `0005` 주석으로 옮겼다.** `0004` 에는 "0005 가 대체함"만 남겼다.
여러 벌이 남아 있으면 오래된 쪽을 복사해 **새 컬럼이 조용히 버려진다.** 앞으로도
컬럼이 늘면 가장 나중 마이그레이션 주석으로 옮겨 적는다.

#### 목록 정렬 — 무평점을 0 으로 보지 않는다

`sortByRating`(`lib/toilets/cluster.ts`)은 **평점 있는 곳을 먼저** 두고 그 안에서
높은 순으로 세운다. 평점이 없는 곳은 **원래 순서 그대로 뒤에** 붙는다
(`Array.prototype.sort` 는 안정 정렬이다).

**무평점을 `0` 으로 취급해 섞지 않는다.** 그러면 "아직 평가 없음"이 "나쁨"으로
읽힌다 — `ToiletListSheet` 가 평점 없는 행에 배지를 안 그리는 이유, 청결도 미터가
0건일 때 5칸을 회색으로 두는 이유와 같은 선이다.

목록 컴포넌트가 아니라 `cluster.ts` 에 둔 것은 이미 그룹을 다루는 모듈이어서다.
**평점이 늦게 와서 목록이 한 번 다시 정렬된다** — `fetchRatingsFor` 는 그룹이
열릴 때 시작한다. 로딩 상태를 따로 만들지 않았다. 캠퍼스 목록은 3~8행이고 지금은
대부분 평점이 없어 순서가 아예 안 바뀐다.

#### 「이 위치에 화장실 추가」 — 마찰이 제보를 그만두게 만든다

빠진 층을 발견한 사람이 추가하려면 지금까지는 **시트를 닫고 `+` 를 눌러 지도를
다시 끌어 핀을 맞춰야 했다.** 이미 그 건물 좌표를 보고 있는데 처음부터 다시
하는 셈이다. 4번 "유도"의 가장 큰 마찰이 여기였다.

- **목록과 상세 시트 양쪽에 둔다.** 한 좌표에 한 곳뿐이면 목록을 건너뛰고 상세가
  바로 뜨므로(1곳짜리는 목록을 안 거치는 것이 2026-08-10 의 결정이다), 목록에만
  두면 **첫 화장실이 막 등록된 건물에서 "다른 층을 추가할 입구"가 사라진다.**
  정석 B1 남자만 넣으면 여자 화장실을 보탤 자리가 없어지는 것이 정확히 이 경우다.
- **역지오코딩을 하지 않는다.** 좌표가 같으면 주소도 같으므로 그 행의
  `road_address`/`jibun_address` 를 그대로 넘긴다. 호출도 대기 시간도 0 이다.
- **건물명 프리필은 행의 `building` 이 있을 때만.** `name` 에서 끌어오면
  `인하대학교` 같은 부지 이름이 건물칸에 박힌다. 지도에서 직접 고른 위치는 항상
  `null` 이라, 핀을 옮겨 놓고 앞 건물 이름으로 제보하는 일이 구조적으로 막힌다.
- 「위치 다시」는 **그 좌표로 지도를 옮기고 나서** 핀 화면으로 돌아간다.
  `panTo` 가 아니라 `setCenter` 인 이유는 애니메이션 중간 좌표를 읽지 않기
  위해서고, 타입 선언에도 `panTo` 가 없다.
- 50m 중복 경고는 **그대로 뒀다.** 같은 건물 다른 층은 정상이라 막지 않고 경고만
  하는 현재 동작이 여기에 딱 맞는다.

#### 검토 중인 내 제보 — RLS 를 안 건드리는 방법

제보는 검토 큐에 쌓이고 승인 전에는 지도에 안 뜬다. **보낸 사람 눈에는 아무 일도
일어나지 않은 것과 같아서 두 번째 제보를 안 한다.** 학생들이 캠퍼스를 채우게
하려면 여기가 막히면 안 된다.

**`toilet_submissions` 에 SELECT 정책을 열지 않았다.** 그게 없는 것이 장난 제보가
검토 없이 남에게 보이는 걸 막는 장치다(2026-08-04 절의 RLS 표). 열려면 "내 것만"을
가릴 `user_id` 가 필요해지는데, 그러면 **"제보에는 신원을 요구하지 않는다"는 결정이
통째로 뒤집힌다.** 그럴 필요가 없다 — **내 브라우저가 방금 무엇을 보냈는지는 내
브라우저가 안다.**

`lib/toilets/my-submissions.ts` 가 localStorage(`bbung.my-submissions`)에
`{lat, lng, name, at}` 를 담는다.

- **`useSyncExternalStore` 용 외부 저장소로 만들었다.** 스냅샷 참조를 고정해
  헛렌더를 막는다. effect 에서 읽어 setState 하는 방식은 **`npm run lint` 가
  `react-hooks/set-state-in-effect` 오류로 잡는다** — 이 프로젝트에 이미 둘
  (`ThemeToggle.tsx`, `ToiletMap.tsx` 의 `initialAdd`) 있어서 셋째를 안 만들었다.
- **승인되면 스스로 지워진다.** 승인 SQL 이 `s.lat, s.lng` 를 그대로 복사하므로,
  조회된 `toilets` 에 **좌표와 이름이 같은 행**이 나타나면 그것이 승인된 내 제보다.
- 30일이 지난 것도 지운다 — 반려된 제보가 영원히 점선으로 남지 않게.
- **경로 캐시가 아니다.** TMAP 24시간 보관 금지와 무관하다. 내가 쓴 글의 사본이다.

표시는 **점선 테두리 + 흰 바닥 핀**이다. 색만 다르게 하면 "다른 종류의 화장실"로
읽힌다. **점선은 앱 안에서 이미 같은 말을 하고 있다** — 추가 버튼과 "정보 없음"
칩이 점선이다. 마커 이미지 캐시 키가 `${state}:${count}` 라 상태 하나를 더하는
것으로 끝났다.

누르면 상세가 아니라 **4초짜리 안내**다(`검토 중이에요 · 승인되면 지도에
올라갑니다`). 열어 줄 데이터가 내가 적은 것뿐이고 리뷰를 달 대상도 아직 아니다.
제보 성공 화면도 점선 핀을 예고한다 — 예고가 없으면 그 핀이 무엇인지 모른다.

#### 좌표 딥링크 (`lib/map/focus.ts`)

`?lat=&lng=&level=` 로 지도를 그 좌표에서 연다. 사이드바의 **「인하대 화장실」**
이 이 링크를 쓴다(`/?lat=37.4506&lng=126.6536&level=5`).

**`?campus=inha` 로 안 만들었다.** 좌표 링크는 어디에나 쓸 수 있어 **P1 12번(위치
링크 공유)이 그대로 가져다 쓴다.** 학교 이름을 주소에 새기면 학교가 늘 때마다
코드에 이름을 하나씩 더해야 하고, 그 목록을 들고 있는 곳이 또 하나 생긴다.
**인하대 전용 화면도 안 만들었다(사용자 결정)** — 전국 앱에 학교 하나만
특별대우하는 화면이 생기면 다음 학교가 들어올 자리가 없다.

- 좌표 둘 중 하나라도 이상하면 **통째로 무시**하고 평소대로(내 위치에서) 연다.
  반쪽짜리 좌표로 엉뚱한 바다를 열어 주느니 낫다. **축척만 이상한 경우는 좌표를
  살리고 축척만 버린다** — 어디를 보여줄지는 이미 알고 있으니까.
- **`center` 에 넣으면 안 된다.** `ToiletMap` 의 `center` 는 "내 위치"라서
  `anchored ? center : null`·`boundsAround(center, NEARBY_RADIUS_M)`·파란 점이 전부
  그 값을 본다. 딥링크를 거기 넣으면 **인하대에 내가 서 있는 것처럼 계산한다.**
  그래서 지도 생성 옵션과 별도 effect 로만 들어간다.
- **위치를 받아도 지도를 안 옮긴다**(`holdFocusRef`). 안 그러면 몇 초 뒤 지도가
  내 위치로 튀어 링크가 무효가 된다 — 인하대를 보라고 준 링크인데 내 방이 열린다.
  파란 점·반경 계산·조회는 그대로 돈다. **중심만 안 옮긴다.**
- **잠금은 `requestLocation()` 이 푼다.** 한 번만 건너뛰는 플래그로 만들면
  거부→재시도→허용 순서에서 그 한 번이 소진돼 지도가 붙박이가 된다. 「현재
  위치로 이동」을 직접 누른 사람에게는 지도가 따라와야 한다 — 링크보다 그쪽이
  나중이자 명시적인 의사표시다.
- **지도를 만들 때 이미 반영되는데도 effect 가 따로 있다.** 지도를 보고 있는 채로
  사이드바 항목을 누르면 **주소만 바뀌고 이 컴포넌트는 그대로 있다.** `?add=1` 에
  마운트 effect 를 따로 둔 것과 같은 이유다. 그리고 그 effect 는 아래 recenter
  effect 보다 **먼저 선언돼 있어야 한다** — 같은 커밋에서 둘 다 도는
  `sdkReady` 순간에 잠금이 먼저 걸려야 한다.
- **좌표를 객체가 아니라 값으로 푼다.** `parseFocus()` 는 서버가 렌더할 때마다
  새 객체라 의존성에 그대로 넣으면 아무것도 안 바뀌었는데 지도가 다시 움직인다.

#### 시드 2건

`scripts/data/inha-toilets.csv` 에 **정석학술정보관 지하 1층 남자**·**하이테크관
2층 남자** 두 줄. 빈칸은 전부 "모름"이다 — `n` 을 기본값으로 삼으면 안 적은
건물이 전부 "장애인 화장실 없음"이 된다(2026-08-10 절의 같은 판단).

건물 이름은 **카카오 키워드 검색이 찾을 수 있어야 하고**, 캠퍼스 중심 1.2km 밖
결과는 거부되므로 못 찾으면 그 행이 건너뛰어진다 — 로그를 보고 이름을 고친다.

**2026-08-18 에 투입했다.** 계획서는 "service role 키가 로컬에만 있어 사용자가
돌려야 한다"고 적었는데, 그 키는 **이 작업 환경의 `.env.local` 에 있다** — 사용자
몫이 아니었다. 두 건물 다 한 번에 찾혔다(정석학술정보관 `37.449392,126.652484` ·
하이테크관 `37.450710,126.657121`, 둘 다 캠퍼스 안).

#### 검증 (2026-08-17)

각 단계 끝에 로컬 프로덕션 빌드로 390×844·1440×900 을 봤다. **콘솔 에러는 로컬
전용 `/_vercel/insights/script.js` 404 뿐**이고 지도 컨테이너 높이는 844/900 이었다.
**Supabase 조회를 전부 가로채 가짜로 답했다** — 프로덕션 DB 에는 아무것도 안 썼다.

| 단계 | 확인                                                                                          |
| ---- | --------------------------------------------------------------------------------------------- |
| 1    | 4단 성별 칩 · 제보 행의 `gender`/`unisex` 가 서로 맞음 · 승인 SQL 로 성별이 `toilets` 까지 감 |
| 2    | 평점 있는 행이 위 · 없는 행은 원래 순서 · 목록과 상세 양쪽에 버튼 · 폼이 채워진 채 바로 뜸    |
| 3    | 제보 직후 점선 1개 · 새로고침해도 남음 · 승인 행을 끼우면 점선 0·진짜 마커 2·저장본 0         |
| 4    | 첫 페인트부터 인하대 중앙 · 위치가 와도 그대로 · `level` 빼면 핀 간격 139→278px               |

**검증 함정 셋. 전부 측정 쪽 버그였고 제품 코드는 멀쩡했다.**

- **조회 요청의 bbox 로 지도 중심을 재면 안 된다.** `fetchToilets` 가 지도 영역에
  `현재 위치 ± 1km` 를 **합쳐서** 보내므로, 지도가 인하대이고 내 위치가 서울이면
  둘을 다 덮는 상자가 나간다. 첫 시도에서 두 곳의 중점이 찍혀 "실패"로 오판했다.
  **화면에 실제로 보이는 핀 끝**으로 재는 것이 확실하다(마커 이미지 기준점이
  가로 중앙·세로 맨 아래라 그 점이 곧 좌표다).
- **창 중앙과 비교하면 안 된다.** 데스크톱에는 왼쪽에 256px 사이드바가 있어 지도
  중심이 창 중심보다 **128px 오른쪽**이다. `main > div` 의 사각형으로 재야 한다.
- **화면 밖 마커는 사각형이 전부 0 으로 나온다.** 그걸 위치로 읽으면 헛수치가
  된다. `width > 0 && height > 0` 로 거르면, 그 **"안 보임" 자체가 "지도가 거기서
  떠났다"는 긍정적 증거**가 된다.

점선 핀은 `encodeURIComponent` 가 `stroke-dasharray` 를 그대로 두므로 마커
`<img>` 의 data URI 를 문자열로 뒤져 셀 수 있다.

### 「인하대 화장실」을 눌러도 아무것도 없다 (2026-08-18)

배포 직후 사용자 제보 — **"인하대 화장실 누르면 아무것도 안나오는데 이거 맞아?"**
**딥링크는 멀쩡했다. 캠퍼스가 진짜로 비어 있었다.**

기능부터 의심하지 않고 **데이터를 먼저 봤어야 했다.** 8/15 에 부지 단위 3건을
지웠고(위 절) 시드는 한 번도 안 돌았으니 캠퍼스 안은 **0곳**이 맞다. 실제로
`lat/lng ±0.003°`(약 300m) 조회가 빈 배열이었다. 그 자리에 「인하대 화장실」이라는
메뉴를 놓았으니 **앱이 약속한 것과 데이터가 어긋나 있었다.**

딥링크 쪽은 네 경우를 재현해 전부 정상이었다(로컬 프로덕션 빌드, 실제 DB).

| 경우                    | 보이는 마커 | 중앙에서 가장 가까운 핀 |
| ----------------------- | ----------- | ----------------------- |
| 주소로 직접 열기        | 48          | 97px                    |
| 지도를 보다가 클릭      | 48          | 97px                    |
| 위치 거부 상태에서 클릭 | 48          | 97px                    |
| 데스크톱(1440)에서 클릭 | 167         | 97px                    |

프로덕션 HTML 로도 확인했다 — 좌표 없이 열면 `37.4506` 이 **1번**(사이드바 링크
href)뿐이고, 딥링크로 열면 **5번**으로 늘어 focus 값이 실려 나간다.

**시드를 넣으니 바로 풀렸다.** 가장 가까운 핀이 **97px → 41px** 로 당겨지고
(정석학술정보관까지 약 166m), 그 핀을 누르면 `정석학술정보관 지하 1층 남자` 가
건물·층·성별까지 붙어 열린다.

> **교훈 — 지도가 비어 보인다는 제보는 "지도가 고장났다"가 아닐 수 있다.**
> 이 앱은 화면과 데이터가 따로 배포된다(코드는 push, 행은 SQL·스크립트). 화면을
> 먼저 뒤지기 전에 **그 좌표에 행이 있는지부터** 조회할 것.

**같이 발견한 것 — 멀리서 딥링크를 열면 마커가 잘린다. 아직 안 고쳤다.**
내 위치가 서울(27km 밖)인 채로 인하대 링크를 열면 조회가 `MAX_ROWS`(300)에 걸려
**보이는 마커가 48 → 19 로 준다.** 상한에 닿았을 때 다시 받는 것이 **기준점
주변(내 위치 ± 1km)뿐**이라, 정작 링크가 가리킨 화면은 잘린 채로 남는다(위
"공공데이터 적재"의 bbox 절이 만든 보정이 딥링크를 모른다). 앱바도 `1km 이내
70곳` 이라며 **인하대가 아니라 내 위치 주변**을 센다.

지금은 급하지 않아 뒀다 — 홍보 대상인 인하대생은 대부분 근처에서 연다. 고칠
때는 **보정 재조회를 기준점 하나가 아니라 "지도 중심"에도 걸어야 한다.**

### 다음 할 일 (2026-08-18 기준)

**배포·마이그레이션·시드까지 끝났다. 남은 사람 몫은 하나다.**

1. **Vercel Web Analytics 켜기.**

그 다음이 **실기기 에브리타임 웹뷰 확인**이다.

캠퍼스는 **2곳뿐이다.** 나머지 건물은 학생 제보로 채우는 것이 설계지만, 직접 더
넣으려면 `scripts/data/inha-toilets.csv` 에 줄을 더하고 `npm run seed:campus` 를
다시 돌린다(`external_id` upsert 라 중복이 안 생긴다).

> Web Analytics 는 Vercel 대시보드 **왼쪽 사이드바의 `Analytics`** 에 있다
> (프로젝트 Settings 안이 아니다). `Enable` 후 **재배포해야** `/_vercel/insights/*`
> 경로가 붙는다.

**남은 P0 는 6번(사진 업로드) 하나다.** **신고·삭제 절차가 같이 와야 한다** —
익명 신원에는 부적절한 사진의 책임을 물을 수 없다(`idea.md` 유의사항 ①).
그때가 `linkIdentity` 로 카카오를 붙일지 판단할 자리이기도 하다.

**출시 후에 숫자를 보고 정할 것 셋.**

- **TMAP 실사용량** — 폴백이 얼마나 자주 뜨는지. 잦으면 메모리 캐시나 한도 상향.
- **에브리타임 유입** — referrer 가 실제로 잡히는지. 안 잡히면 홍보 효과를 못 본다.
- **제보와 리뷰가 들어오는지** — Supabase 에서 SQL 로 센다. 들어오기 시작하면
  **승인 UI 가 없는 것이 바로 운영 부담이 된다.**

**P1 은 아직 하나도 없다.** 검색·필터·즐겨찾기·공유. 사용자가 실제로 무엇을
아쉬워하는지 보고 고르는 편이 낫다. 다만 **12번(위치 링크 공유)은 절반이 이미
있다** — 좌표 딥링크가 받는 쪽을 다 하고 링크를 만드는 버튼만 없다.

**범위를 먼저 좁힐 것.** 한 번에 하나씩, 끝나면 커밋하는 이 프로젝트의 방식을
유지한다.

**끝난 것들** — P0 1·2·3·4·5(리뷰 등록은 2026-08-05 익명 인증으로 풀렸다) ·
P0 7번은 취소(익명 인증이 대신한다) · 화장실 추가(2026-08-04) · 전국 데이터·
마커 겹침(2026-08-10) · 캠퍼스 준비(2026-08-17).

**빈 상태는 부분적으로 풀렸다** — 청결도 미터가 리뷰가 없을 때 "아직 평가 없음"
으로 채워질 자리를 보여준다. `ReviewList` 의 "아직 리뷰가 없어요"는 그대로다.

**운영 부담 하나** — **제보가 들어오면 Supabase 대시보드에서 SQL 로 승인해야
지도에 뜬다.** 쿼리는 **가장 나중 마이그레이션 주석이 정본**이고, 지금은
`supabase/migrations/0005_gender.sql` 이다. 컬럼이 늘 때마다 거기로 옮겨 적는다 —
여러 벌이 남아 있으면 오래된 쪽을 복사해 새 컬럼이 조용히 버려진다.

**승인이 밀리면 점선 핀도 안 사라진다.** 제보한 사람 화면에는 그 자리가 계속
"검토 중"이고, 30일이 지나면 저장본이 스스로 지워져 **반려된 것과 구분이 안 된다.**

> DDL 은 앞으로도 **코드에서 못 돌린다.** `.env.local` 에 DB 접속 문자열이 없고
> service role 키는 PostgREST 용이다. 새 마이그레이션은 항상 사용자가 Supabase
> SQL Editor 에 붙여 Run 해야 한다.

UI 네 건이 **전부 끝났다** — 배포 지도 미표시·위치 정확도·반경 표시(2026-08-01),
그리고 미뤄 뒀던 **다크 모드 토글(2026-08-07, 66db7e7).**

미해결로 남은 것 하나 — **실제 데스크톱의 `accuracy` 값을 아직 못 받았다.**
사용자가 배포된 앱을 확인했지만 그 값은 전달되지 않았다. 오차 원이 다시 문제가
되면 "현재 위치가 부정확한 문제" 절의 "남은 의문"부터 읽을 것. 근접 제한을 지운
뒤로 이 값은 **리뷰와는 무관하다** — 남는 영향은 오차 원·반경 표시·직선거리다.

**리뷰 수정 기능은 일부러 없다.** `reviews` 에 UPDATE 정책을 두지 않았다. 수정하면
`created_at` 이 "최종 확인 시점"을 잘못 나타내기 때문이다. 삭제 후 재작성으로
처리한다. **삭제 UI 도 아직 없다** — 필요해지면 그때 만든다(정책은 이미 있다).

**RLS 를 풀어 우회하지 말 것** — 아무나 남의 이름으로 쓰고 지울 수 있게 된다.

### 환경변수 — 어디에 두는지

Vercel에 등록할 것은 앱이 **런타임·빌드에 실제로 쓰는 것만**이다.
`scripts/`용 키는 로컬 `.env.local`에만 두고 Vercel에 올리지 않는다.

| 키                                          | Vercel           | 쓰는 곳                       |
| ------------------------------------------- | ---------------- | ----------------------------- |
| `NEXT_PUBLIC_KAKAO_MAP_KEY`                 | 필요             | 브라우저 지도 SDK             |
| `NEXT_PUBLIC_SUPABASE_URL` · `..._ANON_KEY` | 필요             | 브라우저 조회                 |
| `TMAP_APP_KEY`                              | 필요             | `app/api/walk-route` 런타임   |
| `KAKAO_REST_API_KEY`                        | 불필요           | `npm run geocode`             |
| `DATA_GO_KR_SERVICE_KEY`                    | 불필요           | `npm run collect` (디코딩 키) |
| `SUPABASE_SERVICE_ROLE_KEY`                 | **올리지 말 것** | RLS를 통째로 우회함           |

`NEXT_PUBLIC_*`은 빌드 시 클라이언트 번들에 문자열로 박힌다. Vercel에서
Sensitive로 표시해도 배포된 JS를 열면 보이므로 의미가 없다. 카카오 JS 키는
노출이 전제이고 **도메인 등록**으로 보호한다 — 그래서 포트가 다르면
(`localhost:3111` 등) SDK가 거부된다. 로컬 확인은 등록해 둔 포트로 할 것.

### 색은 globals.css 에서만 (a74815f)

컴포넌트에 `zinc-500`·`emerald-600` 같은 팔레트 색을 직접 쓰지 않는다.
`app/globals.css`가 CSS 변수로 색을 정하고, Tailwind v4의 `@theme inline`이
그걸 유틸리티 이름으로 바꿔준다. 컴포넌트는 이름만 쓴다.

| 이름                                         | 뜻                                 |
| -------------------------------------------- | ---------------------------------- |
| `paper`                                      | 화면 바닥                          |
| `surface`                                    | 앱바·시트·카드                     |
| `sunken`                                     | 입력칸처럼 한 단 들어간 면         |
| `ink` · `muted`                              | 본문 글자 · 보조 글자              |
| `line`                                       | 테두리                             |
| `brand` · `brand-strong` · `brand-ink`       | 청록, 눌렀을 때, 그 위에 얹는 글자 |
| `brand-soft` · `-ink` · `-line`              | 연청자 배경의 강조 칩              |
| `signal` · `signal-soft` · `signal-soft-ink` | 경고·오류 (주황)                   |
| `star` · `star-empty`                        | 별점                               |

색이 아닌 토큰 둘도 같은 파일에 있다(2026-08-07 추가, 위 "Stitch 디자인 이식").

| 이름                                            | 뜻                                        |
| ----------------------------------------------- | ----------------------------------------- |
| `shadow-float` · `shadow-sheet` · `shadow-chip` | 지도 위에 뜬 면의 그림자 (청록)           |
| `text-label`                                    | 섹션 헤더 (12px/700/자간, uppercase 없음) |

**그림자 토큰은 `:root` 에서 `--elev-*` 라는 이름으로 산다.** `--shadow-*` 는
Tailwind v4 가 유틸리티를 만드는 네임스페이스라 거기에 원본 값을 두면 부딪힌다.
`--paper` → `--color-paper` 와 같은 방식으로 `@theme inline` 에서 이어 붙인다.

**다크 모드에 `dark:` 변형을 쓰지 않는다.** 값은 `light-dark(라이트, 다크)`
한 줄로 적고 컴포넌트는 이름만 쓴다. `dark:`를 쓰기 시작하면 색 하나를 고치는
데 파일 열 개를 열어야 하고, 두 모드가 서서히 어긋난다. **두 벌을 따로 적는
것도 같은 이유로 안 한다** — 2026-08-07 에 `@media (prefers-color-scheme: dark)`
블록을 지우고 `light-dark()` 로 옮겼다(위 "Stitch 화면 구조 이식" 절).

**어떤 테마인지는 `<html>` 의 `data-theme` 이 정한다.** 속성이 없으면
`color-scheme: light dark` 라 OS 를 따라가고, 그것이 "시스템"이다.

예외는 **지도에 넘기는 색**이다. 카카오 SDK의 Polyline 색이나 마커 SVG의
데이터 URI 안에서는 CSS 변수가 풀리지 않아 리터럴을 쓸 수밖에 없다
(`ToiletMap.tsx`의 `ROUTE_COLOR`, `lib/map/toilet-marker.ts`의 `BODY`).
`globals.css`의 `--brand`를 바꾸면 이 두 곳도 같이 고쳐야 한다.

### 지도 레이아웃 주의 (같은 버그 재발 방지)

지도가 흰 화면이었던 원인은 SDK가 아니라 **높이 0**이었다. `body`가 `min-h-full`,
`main`·래퍼가 `flex-1`(= `flex-basis:0%`)이면 flex가 계산한 높이는 퍼센트 해석의
기준이 못 되고, 지도 컨테이너의 `h-full`이 `auto`로 무너진다. 카카오 내부 요소는
전부 `position:absolute`라 내용 높이가 0 → 지도는 정상 생성·타일 로드까지 되고도
`overflow:hidden` 박스에 갇혀 안 보인다.

- 지도까지 이어지는 높이는 `flex-1`이 아니라 **`h-full`로 잇는다.**
- 지도 위에 얹는 배너·오버레이는 **`z-10` 이상**을 준다. 카카오 내부 레이어가
  z-index 1·2를 쓰므로 `z-index:auto`면 덮인다.
- 확인 방법: `npm run build && npm start` 후 브라우저에서 지도 컨테이너의
  `getBoundingClientRect().height`가 0이 아닌지부터 본다. 콘솔 에러는 안 난다.

## 기술 스택

- 프론트엔드: Next.js (React) + Tailwind
- 지도: 카카오맵 SDK (표시·마커·폴리라인) + 카카오 REST (주소→좌표 지오코딩)
- 도보 경로: TMAP 보행자 경로안내 API (일 1,000건 무료)
  - 약관상 응답을 24시간 이상 보관할 수 없다. 경로를 DB·localStorage에 저장하지 말 것.
- 백엔드: Next.js API Routes
- DB·인증·스토리지: Supabase
- 배포: Vercel (GitHub 연동)

## 개발 원칙

- 지금은 P0(MVP 핵심) 기능만 만든다. P1·P2는 나중.
- 바로 코딩하지 말고, 먼저 계획을 세워 확인받은 뒤 구현한다.
- 기능은 작게 쪼개서 하나씩 진행하고, 한 기능이 끝나면 커밋한다.
- 커밋은 기능 단위로 나누고, 무엇을·왜 바꿨는지 메시지에 남긴다.

## Git · 배포 규칙 (중요)

- 이 프로젝트는 GitHub에 올리고 Vercel로 배포한다.
- Vercel은 GitHub의 main 브랜치에 push되면 자동으로 배포한다.
  따라서 main은 항상 실행 가능한(빌드되는) 상태를 유지한다.
- API 키·비밀값(카카오·Supabase 등)은 절대 커밋하지 않는다.
  - 로컬은 `.env.local`에 저장하고, `.gitignore`에 반드시 포함한다.
  - 배포용 값은 Vercel 대시보드의 환경변수(Environment Variables)에 등록한다.
- 커밋·푸시 전에 diff를 확인해 키나 `.env`가 섞이지 않았는지 점검한다.

## 코드 컨벤션

- Next.js는 16.x다. 학습 데이터와 API·규약이 다를 수 있으니
  코드를 쓰기 전에 `node_modules/next/dist/docs/`의 해당 문서를 확인한다. @AGENTS.md 참고.
- App Router + TypeScript + Tailwind v4. `src/` 없이 루트에 `app/`을 둔다.
- 폴더 구조
  - `app/` — 페이지·레이아웃, `app/api/` — API Routes
  - `components/` — 재사용 UI 컴포넌트
  - `lib/` — Supabase 클라이언트 등 공용 모듈
  - `scripts/` — 데이터 수집·지오코딩 등 로컬 실행 스크립트 (배포에 포함되지 않음)
  - `supabase/migrations/` — DB 스키마 SQL
- 파일명: 컴포넌트는 PascalCase(`KakaoMap.tsx`), 그 외는 kebab-case(`geocode-toilets.ts`).
- 임포트는 `@/` 별칭을 쓴다 (`@/lib/supabase/client`).
- UI 문구는 한국어. `lang="ko"`, 폰트는 시스템 한글 스택.
